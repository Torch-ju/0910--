# 开发历史与证据

这里只保存已经发生的事件及结果；当前进度唯一见 CURRENT。同一 operation_id 只有一个事件标题，重试更新原事件结果；新目标使用新 ID。

事件字段至少包含日期、任务、目标、状态、输入依据、文件影响、检查结果和遗留项。检查使用 PASS / FAIL / NOT_RUN；不能用计划或文档要求替代检查结果。

## OP-20260912-002

- 日期：2026-09-12。
- 状态：STARTED。
- 任务：L1-01 至 L1-06。
- 来源：用户明确授权实施最终计划；两个场景、动态 NPC、世界历史与开局时间线，10 次真实联调上限。
- 已执行：创建 Next.js 工程配置、三个 JSON Schema、生成类型、共享接口、实例工厂和校验层，安装依赖。
- 凭据：用户本地环境文件中维护，不记录或复制至此。初选模型属图像生成，替代文字模型待用户选择；生成调用为 0。
- 检查进展：状态/契约 23、Agent 19、UI 3，总计 45 个离线测试通过；typecheck 通过。UI 字段 JSON Pointer、候选前后对比、锁定与失败输入保留已修正。
- 真实联调：用户已确认文字模型 doubao-seed-2-0-pro-260215。首次框架请求 op_092b8680f6414f92a3e4c6570d7c45ba 在 60 秒超时，状态 uncertain，账本 used=1；没有自动重试。公开 GET 到同服务为 HTTP 200/491ms。
- 环境修正：Next.js 遥测全局文件 rename EXDEV，通过启动子进程禁用遥测；自动追加 AGENTS 规则通过 agentRules:false 关闭并移除自动片段。ESLint 10 与 Next 内置 React 插件接口不兼容，回锁 9.39.5；其维护状态警告记录为开发工具限制。
- 待完成：最终 lint/构建、真实生成与浏览器多宽度及保存/导出。

## OP-20260912-001

- 日期：2026-09-12，Asia/Shanghai。
- 任务：L1-00。
- 目标：将开发提示词整理为唯一功能规格，建立项目 AGENTS、README、memory 与幂等接续索引。
- 状态：CHECKED。
- 发起依据：用户当前明确要求整理开发文档并维护唯一开发记忆库。
- 需求来源：本对话关于共同创作、第一层、简约 UI 和正式 JSON Schema 的确认；既有提示词全文及已知早期架构。
- 本轮目录依据：只读根目录列出四份原始文档，没有源码或 Schema 目录；随后按本轮补丁创建文档目录。

### 文件影响

- `docs/PRODUCT_SPEC.md`：由旧提示词迁移，保留产品主线与详细要求，增加稳定功能 ID，统一 Schema/实例和角色关系说明，规定未来更新幂等。
- `第一层产品开发完整提示词.md`：改为旧路径导向，不再复制功能正文。
- `ARCHITECTURE (1).md`：仅加历史架构定位，避免其中的其他工程路径被当成现有源码。
- `AGENTS.md`：项目协作、唯一职责、操作 ID、检查点和恢复协议。
- `README.md`：产品入口、文档导航和接续说明；不虚构启动命令。
- `memory/INDEX.md`：文件职责与功能、决策、任务、操作 ID 的索引。
- `memory/DECISIONS.md`：D-001 至 D-009，区分用户决定与工程选择。
- `memory/CURRENT.md`：建立 L1-00 至 L1-06 的唯一任务表。
- `memory/HISTORY.md`：本事件；后续重试复用 OP-20260912-001。

### 检查

| 项目 | 结果 | 证据或限制 |
| --- | --- | --- |
| 整理前工作区文件核对 | PASS | 根目录只读结果为四份文档 |
| 文档补丁 | PASS | apply_patch 成功回执；产品代码未生成 |
| 本地链接、唯一编号与规格说明 | PASS | 9 份文档、25 个本地链接；10 个功能、12 个验收场景、7 个任务、9 个决定、1 个操作 ID，数量与唯一性一致 |
| 文档 JSON 代码块语法 | PASS | 5 个代码块可被 JSON 解析；结构示意仍不是通过正式 Schema 校验的数据实例 |
| 应用测试、构建、浏览器 | NOT_RUN | 应用工程尚未创建 |
| Schema 编译、实例验证、真实 AI | NOT_RUN | 只整理契约需求，尚无独立实现 |

### 环境注记

本轮普通 exec 在沙箱 helper 启动处遇到 `os error 206`。错误发生在命令执行前，不能归因于产品文件或补丁长度。经受批准的 exec，使用此前已经成功的 apply_patch 运行时完成文件更新；未重装环境或修改持久工具配置。以后接续先确认当时可用工具，不依赖永久存在的缓存路径。

### 接续

本轮文档交付与限定检查已完成，检查命令退出码为 0，结果 PASS，无错误。结果回写本事件后同步 CURRENT。产品后续入口为 L1-01；没有启动开发服务器、付费模型调用或外部发布。


## OP-20260912-002 / 收尾检查点：2026-09-12 15:10 后

完成本轮代码的集中检查：Lint、TypeScript、51 项测试、Schema 生成类型一致性和生产构建全部通过。合成浏览器流程已覆盖人物增删合并、字段来源与锁定、时间线编辑、确认、保存失败不丢输入、刷新恢复和配套 JSON 下载。四个要求宽度无横向溢出；390/1440 截图已查看。实例与下载均明确为合成数据，不提升为真实模型成功证据。

真实模型总计使用 5/10 次：前两次大请求超时或网络失败；第三次同模型小文本探针成功；第四次精简协议生成和第五次自动结构修复仍触发“人物线索格式无效”，应用返回 422。停止额外调用并保留剩余 5 次，向用户说明缺口和询问修正许可。输入、失败回执和持久计数保留，没有静默演示结果替代。

停止本任务开发服务后完成生产构建，启动本地生产预览 127.0.0.1:3000。独立生产页面初次加载 0 errors / 0 warnings；状态接口在重启后仍为 used=5 / limit=10。README 改为实际工程入口，CURRENT 整理唯一进度，规格补充内部投影与数据事实边界。整体仍是部分交付，L1-06 未完成。

注意：npm run test:e2e 尚未绑定实际 CLI 脚本，未运行也未声称通过；真实执行的浏览器命令记录在 README。不删除历史错误，不清空模型账本。

## OP-20260912-003：识别契约修正与字段级共同创作

- 用户目标：修正候选无法采用、NPC 不自动生成，以及每个内容字段缺少独立 AI 建议入口。
- 操作状态：CHECKED；部分通过，未完成真实 NPC 闭环。
- 已落盘：共同 StoryContext/FieldTarget/AgentEndpoint；公开 Schema 派生的字段输出契约；按稳定 ID 的候选三方合并；无变化编辑幂等；采用世界后默认请求 NPC；字段局部输入与共同想法分离。
- 当前证据：集中 68 项测试 PASS，随后新增字段组件 5 项独立测试 PASS；生产代码 Lint/类型/构建 PASS；新 UI 四宽度无横向溢出。真实主题字段通过生成、保留后续标题编辑、安全采用、保存和刷新恢复。
- 最终结果：真实第 8 次世界生成网络不明；第 9 次字段建议成功；第 10 次采用既有世界后确实自动发出 NPC 请求，上游 HTTP 200 / 67763ms，但内容校验未通过且修复被 10/10 上限拦截，未产生第 11 次。NPC 结果不可用，整体未完成。
- 历史边界：上轮 51 项通过仅为旧基线；模型账本最新观察 7/10，其中第 6 次世界生成、第 7 次世界局部修改已有成功结果。未删除早期失败。

- 本轮明确剩余问题：申请修复额度失败时覆盖了原始 Schema 错误；NPC 接续只检查 recognition 关键问题，未同时检查 world.open_questions。已告知用户，未擅自继续另一轮修补或增加额度。
- 合成批量浏览器脚本因 CLI 执行环境没有全局 URL 报错，记录 FAIL，不把未执行的断言写成通过。
- 当前生产服务：127.0.0.1:3000，exec 13266。用户浏览器没有被清空或强制刷新。已保存草稿可继续手动编辑，付费生成因额度用尽被禁用。


## OP-20260912-004：首屏简化、清空数据与 Git 分支发布

- 日期：2026-09-12。
- 状态：CHECKED。
- 用户目标：去掉不需要的开头展示，补充清空数据按钮，说明本地打开方式，并将当前项目推送到 GitHub 的 cachmeiss7 分支。
- 已落盘：移除 Provider 顶层的自动 NPC 设置和关系编辑块，将澄清问题回答入口收回对话区；移除装饰性 sw-hero 开场区，将故事风格选择移到故事输入区；关系编辑收进 NPC 工作区折叠区；新增清空当前故事、候选和本地备份的动作与确认提示。
- 清空实现：删除 shuzhongren.workspace.v1 及其 .archive. 本地键；清空时使排队的自动保存失效，避免旧快照写回；模型请求账本不受影响。
- 文档：README 增加浏览器打开地址、Next.js 页面入口和主要修改文件说明。
- 检查：npm test -- --run 6 个文件 / 75 项通过；npm run lint、npm run typecheck、npm run build 通过。清空回归测试确认删除后 600ms 内不会由自动保存重新创建。
- Git：远程 origin/main 为 bb41e12f；已从该基点创建 cachmeiss7，提交 c01ee3c7 已推送并设置跟踪 origin/cachmeiss7。推送提交不含 .env.local、node_modules、.next、output 或 runtime。
- 本地预览：已执行 npm run dev，启动 exec 会话 93837，http://127.0.0.1:3000 返回 HTTP 200；真实模型额度与历史 NPC 链路状态不变，未发起新请求。


## OP-20260912-005：合并 cachmeiss7 与 peng 到 main

- 日期：2026-09-12；状态：CHECKED（本地合并及验证完成）。
- 授权：用户明确要求把两个分支合并至远端 main；保留来源分支。
- 基点：main bb41e12fec97398affe05ffa4126f32bd8f3372f；来源 cachmeiss7 71adec096b929bef68a250c744ab1b98109543b4、peng cedd40adf55a732525191a5009c53c7f46616eaa。
- 两次普通合并无文本冲突，保留 main 原有资料与来源分支历史。
- 集成修正：根 tsconfig 排除独立 memory-agent，避免未安装其依赖时主应用类型检查报 pg 缺失；修正前复现 FAIL，修正后移开模块依赖验证 PASS，并恢复依赖。README 补充独立模块入口。
- PASS：主应用 6 文件 / 74 项测试、Lint、类型检查、Schema 类型一致性、JSON 样例校验、Webpack 生产构建；memory-agent 12 项测试、类型检查与构建；git diff --check。
- 环境限制：默认 Turbopack 构建因创建子进程绑定端口报 Operation not permitted，提升执行权限后仍失败；npm run build -- --webpack 成功。npm 原有缓存权限失败后使用临时缓存成功。
- NOT_RUN：真实模型、数据库及浏览器端到端联调。合并不表示 memory-agent 已接入工作台，也不改变此前 NPC 链路未验收的状态。
- 发布：本检查点随普通 main 推送发布；推送后需从远端确认两个源提交均为 main 祖先及 PR #1 状态。


## OP-20260912-006：完整产品 PRD 与主 Agent 编排

- 日期：2026-09-12；状态：CHECKED。
- 授权：整合当前工作区全部资料与 Agent，生成完整 PRD、调用规则保存根目录，并实现主 Agent 串联逻辑。
- 工件：工作区 ../PRD.md、../MAIN_AGENT_RULES.md；src/lib/orchestration 的主状态机、子 Agent 适配、提示词、会话存储和记忆桥接；/api/story/main；旧四个设定 API 统一路由；转写输入/输出 Schema 与示例；文档入口与规则更新。
- 本地原有 transcription-agent.zip 删除及展开目录保留；本轮修改展开材料以对齐可执行契约，未删除 .DS_Store 或其他用户文件。
- 主链路：角色→旁白→转写→记忆抽取→确定性记忆更新→摘要→章节检查；逐步检查点、失败停止、显式重试、操作指纹、版本冲突、同故事锁与最终原子提交。
- 初次测试夹具缺少 opening 被旧确认逻辑拒绝，补齐有效夹具并在初始化增加开局检查；不放宽旧契约。
- 初次 webpack 构建 FAIL：memory-agent 的 NodeNext .js 引用无法解析；增加 extensionAlias，并显式使用 webpack 后 PASS。
- npm 离线更新锁文件因无关可选包缓存缺失 FAIL；使用已有已安装/已锁定的 Zod 4.6.2 直接提升运行依赖，未更换包版本或 integrity，npm ls zod --omit=dev PASS。
- PASS：主应用 8 文件 / 90 项测试；新增编排、模型回执及契约检查 16 项；memory-agent 12 项；Lint 无错误/警告；TypeScript；Schema 生成一致性；生产构建，包含 /api/story/main；git diff --check。
- PASS：生产服务 127.0.0.1:3107 合成 API 验证——初始化、查询、相同快照重复初始化、旧版本拒绝、未知动作拒绝。模型调用 0；本次合成故事已删除。服务在验收后停止，不作为后台运行服务。
- 关键恢复证据：摘要失败后新建 MainAgent 实例只重试摘要；最终写盘失败后无额外模型调用完成恢复；人物 ID 跨轮次保持；正文/记忆/章节不重复提交。
- NOT_RUN：真实模型多轮文学与逻辑质量、PostgreSQL 集成、公网部署、正式写作界面与浏览器端到端流程。现有界面保留，新增功能以服务端 API 交付。
- 未进行 Git 提交或远端推送。两份根目录文档在 0910-- Git 仓库外，独立交付时需携带。


## OP-20260912-007 — 完整本地写作产品与验收

- 状态：CHECKED（本地交付；真实验收部分通过）。
- 授权：用户要求实施完整待办，本地优先；真实模型配置与调用获授权，公网目标及 Git 推送另按具体动作处理。
- 工件：WritingApp / MemoryPanel、library/tasks API、管理/备份/上下文/任务/锁模块、记忆检查点、人工修正、模型账本诊断与统计、便携端到端脚本和真实模型验收脚本；完整需求及调用文档纳入仓库。
- 数据保护：既有草稿、用户压缩包删除均保留；.env.example 中用户刚填的配置移至 .env.local，示例与干净临时副本不保留密钥值。
- 初步证据：本地单测 103 项 PASS（后续新增回执恢复、NPC契约测试）；记忆模块 12 项 PASS；类型/Lint/Schema/构建通过；干净 npm ci 下载 473 包后测试构建通过。
- 浏览器：合成模型完整初始化、两轮写作、刷新、摘要失败恢复、章节、作品/人物、Markdown 内容和四宽度 PASS。一次原生下载事件等待超时，已核实先前步骤完成后调整为内容验证，不把超时抹去。
- 真实验收：起初无 .env.local；找到用户填在示例内的配置后迁移。修正测试环境不加载 .env.local 的启动器问题。沙箱网络调用无服务回执，授权联网后武侠世界真实通过，NPC 原始结果两次将对象写成数组；已补完整 WireCharacterProfiles 根 Schema 和精确 /characters 修复提示，30 项设定测试 PASS。后续结果待填。

- 最终证据：108项离线测试、Lint/类型/Schema/构建/diff PASS；记忆12项 PASS；最终浏览器11次合成调用全流程 PASS。
- 真实最终：两种世界/NPC PASS；武侠3轮、魔幻2轮成功，魔幻第3轮记忆网络未知。最后针对性复验3次调用，未重复已完成正文；两种均3轮断言 FAIL，保留全部报告。
- 人工复核发现来源省略、推断过度与用户动作扩写；详见 docs/LOCAL_ACCEPTANCE.md，不宣称语义全通过。

### OP007归档的第一层历史快照

## 历史第一层基线

以下保留原第一层实施记录。旧时间、服务、额度、测试数和待办属于历史证据，不能覆盖上面的本轮结论；运行状态应重新核实。

## 核心语义

- 同一故事的总想法仅使用 snapshot.input；局部要求在 request.input，二者不能混同。
- 每次新请求携带 context.story_idea 与 context.user_notes，并读取现有世界、人物和时间线。
- 字段提示根据目标字段职责区分；输出 Schema 从现有三个公开 JSON Schema 投影，不另建公开 Schema 副本。
- 只有创作 Fact 可请求 AI 建议；ID、版本、锁定和确认权限不能由模型生成。
- 候选保留生成基线；按稳定实体 ID 合并不冲突的后续编辑。同字段冲突、锁定或共同故事想法变化必须明确展示。
- 无实际改变的编辑不增加修订号。不能仅因修订号变化就把所有候选一概拒绝。
- 用户采用世界后默认自动请求 NPC 候选；NPC 候选仍需用户采用，不自动确认。关键澄清、预算不足或生成失败保留已采用的世界。
- 人物数量不固定；动态线索、无名人物、用户控制属性及移除/合并决定继续适用。
- timeline 仍只在世界实例中维护；NPC 引用事件 ID，新增历史仍是待确认建议。

## 高层任务

| 任务 | 状态 | 当前范围 |
| --- | --- | --- |
| L1-01 共同基础 | DONE | 字段契约、共同上下文和共享类型已落盘；相关测试、类型与构建通过 |
| L1-02 共创工作台 | IN_PROGRESS | 字段建议、冲突路径、直接回答和关系编辑已接通；首屏已简化、清空数据已接通；批量浏览器脚本环境问题待修 |
| L1-03 初始化 Agent | IN_PROGRESS | 严格识别 Schema、精确修复提示与动态人物覆盖已实现；真实 NPC 响应尚未通过业务校验 |
| L1-04 状态与一致性 | IN_PROGRESS | 三方合并和自动 NPC 已实现并离线通过；真实字段保留后续编辑与恢复 PASS；世界阻塞问题检查缺口待修 |
| L1-05 模型适配 | IN_PROGRESS | 字段 endpoint 已真实通过；应用层请求次数不再硬限制，原始 Schema 错误保留问题仍待修 |
| L1-06 集成验收 | BLOCKED | 完整世界/NPC真实闭环未通过；本轮未发起新的真实请求 |

## 已有证据与预算

- 上一工作单元：51 项测试、Lint、TypeScript、Schema 类型检查、生产构建及合成浏览器四宽度流程通过。这是历史基线，不替代本轮检查。
- 本轮主 Agent 新增的候选/字段 Schema/controller 12 项测试已 PASS。
- 本轮集中检查：npm test 为 6 文件 / 75 项 PASS；新增首屏/清空数据回归覆盖 2 项。
- 本轮生产代码 Lint、TypeScript 和 npm run build 均 PASS；构建包含 /api/story/field；Git 分支 cachmeiss7 已推送。
- 新生产 UI 的 390/768/1280/1440px 均无横向溢出。
- 第 9 次真实字段请求：主题候选通过校验；生成后修改另一个标题，候选仍可采用且保留标题；保存刷新恢复 PASS，公共故事想法保持不变，主题来源仍为 ai_suggestion / pending_confirmation。
- 新合成批量浏览器脚本 browser-cocreation.playwright：FAIL，Playwright CLI 执行环境缺少全局 URL，尚未完成整条批量流程。不是应用已失败的证明，也不能写成浏览器流程 PASS。
- 真实回放 NPC：用户采用既有成功世界候选后自动触发请求，已验证自动触发；结果校验未过，不代表 NPC 产出可用。
- 模型：doubao-seed-2-0-pro-260215；兼容服务 https://api.openai-next.com；密钥仅本地 .env.local。
- 历史账本曾记录 used=10 / limit=10；本轮已取消应用层次数硬限制，账本继续保留历史计数，不重置历史数据。
- 第 8 次：修正后的独立西方魔幻世界请求出现网络不明错误，未自动重试。
- 第 9 次：独立真实主题字段请求成功并通过采用、无关编辑保留与刷新恢复。
- 第 10 次：基于先前成功世界结果的独立回放，采用世界后自动请求一名“无名引路人” NPC；识别线索是明确测试夹具，不是新识别成功证据。
- 第 10 次操作 op_7f471848e6ac4890891929bae8075c12，上游 HTTP 200，67763ms，promptTokens=4333，completionTokens=2985；内容校验未通过，拟进行的修复被预算上限拒绝，未产生第 11 次模型调用。
- 最终 receipt.failure 只有 request_budget_exhausted，原始校验错误未保留完整，不能推测具体缺字段或以 HTTP 200 当成 NPC 生成成功。
- 回执第 6 次已包含世界和 recognition 结果，第 7 次已包含世界局部修改结果。因此不能继续说后台从未成功生成世界。
- 前五次包含两次网络不明、小文本探针，以及一次世界生成加一次修复失败，历史失败仍保留。
- 原 10 次上限不变，修复与失败都计数；主 Agent 独占本轮真实联调，Worker 不调用模型。

## 服务与恢复

- 当前本地开发预览为 http://127.0.0.1:3000，启动 exec 会话 93837，已用 HTTP 200 验证；运行状态仍以实际进程为准。
- 新构建已启动；用户已有浏览器页需要先保存再刷新，不自动刷新或清空其会话。AI 建议入口不再因本地累计次数禁用，手动编辑与已有数据仍保留。
- 浏览器保存当前及上一份配套快照。没有比对基线且过期的历史候选不能冒险自动合并，提示保留原稿后重新生成。
- 不创建新对话、不对外部署、不清理用户数据、不重置调用预算。

## OP-20260912-008：本地来源误判修复

- 状态：CHECKED。用户截图生成世界框架报 origin_rejected；本机空请求真实复现403。
- 原因：Next 内部 request.url 使用 localhost，而浏览器 Origin / Host 使用127.0.0.1。改用请求 Host 与协议构造目标来源，不信任 forwarded-host；仍拒绝跨站、不同端口/协议及空来源值。
- PASS：2项边界测试、Lint、含类型检查的生产构建；重启服务后本机 Origin 空请求进入400 invalid_request，外站仍403；检查未调用模型。
- 服务：127.0.0.1:3000，exec36254。用户刷新后重试；未改动故事或模型账本。

## OP-20260912-009：完整本地版本发布到 main

- 状态：FAILED（本地提交成功，远端网络阻塞）。
- 授权：用户明确要求将以上全部代码与文件推上仓库主分支。
- 范围：OP006—008代码、完整PRD/调用规则、验收文档、测试脚本、转写展开源码；保留压缩包删除。排除密钥、本地运行数据、依赖与系统缓存，新增 .DS_Store 忽略。
- PASS：本轮109项离线测试、暂存内容密钥检查、diff检查；此前当前代码Lint/类型/生产构建已通过。未追加付费模型调用。

- 本地代码提交 dbe83b7，77个文件变更。远端 fetch 报 HTTP/2 framing error；HTTP/1.1 重试及 IPv4 HTTPS 均连接 github.com:443 超时。普通 git push origin main 30秒超时，无成功回执；未强推，远端状态未确认。网络恢复后先fetch核对再普通push。

## OP-20260912-010：NPC 画像未生成诊断与引导优化

- 状态：CHECKED。用户反馈画像未生成；账本只有一条成功世界回执，无NPC调用；结果含出身/初始地点、拜师/门派两个 blocking 问题。实际浏览器采用状态未直接读取，不能断言已采用。
- 原因：生成链要求采用世界且回答关键问题；原人物页只显示泛化空白文案，回答后缺少明确继续指引。
- 修改：共享 npcBlockingQuestions，人物页区分未采用/关键问题/可生成/候选待采用/生成中状态；可直接回答问题并生成；原按钮与同一阻塞条件一致；答完显示下一步且不自动调用模型。
- PASS：111项离线测试、Lint/类型/生产构建；隔离浏览器复现阻塞→回答→按钮启用且零模型调用；原完整写作流程仍PASS，11次合成调用。未改用户草稿，未发起真实模型生成。

## OP-20260912-011：按输入自动生成画像与时间线

- 状态：CHECKED。用户明确要求NPC画像、世界时间线根据输入自动识别并生成。此规则替代OP010的候选生成前关键问题阻塞；叙事初始化确认条件保留。
- 世界请求生成历史/开局时间线，校验后工作台默认自动以候选世界接续NPC请求；合成一份世界/人物/时间线待采用候选，不提前覆盖正式快照。
- 缺失出身/门派等不阻塞候选，提示模型提出有来源的待确认建议；未来事件仅作为大纲/伏笔。NPC失败保留世界候选并支持独立续试，页面恢复不自动调用模型。
- PASS：112项离线测试、Lint/类型/构建；测试覆盖自动两阶段组合候选、采用不重复生成、旧blocking问题不阻塞、NPC失败独立重试保留世界；隔离浏览器全流程PASS（11次合成调用）。未追加真实模型质量验收。
- 测试服务已更新至127.0.0.1:3000，exec48190；用户草稿未改。

## OP-20260912-012：无逐项确认的自动开篇与创作skill

- 状态：CHECKED。用户要求开始写作后按现有条件直接生成、不限固定字数、内容饱满，并使用指定jin-yong-perspective。
- 使用skill-installer下载到项目vendor/skills，读取SKILL全文；运行时接入核心心智模型与表达DNA，保留来源与SHA256，不执行其中真人身份扮演或外部研究指令。
- 解除逐项确认、关键问题、预填人物/开局对写作的门槛；保留有效JSON/版本/引用和非空想法检查。还修复OP011遗漏的服务端NPC关键问题门禁。
- 开始写作直接使用当前有效候选；先读取已有会话，首次才以稳定ID提交开篇任务，刷新不重发。允许留空续写要求，角色可自主推进主角，故事线与正文基于skill展开完整场景。
- 移除正文16000字符上限与叙事固定max_tokens，供应商单次容量、上下文与存储工程边界仍存在，不能宣称无限响应。
- PASS：113项离线测试、Lint/类型/构建；长正文校验、新初始化门槛、服务端NPC不阻塞、账本回执恢复测试。隔离浏览器自动开篇+留空续写+恢复+导出全流程PASS，16次合成调用。
- NOT_RUN：新提示词真实文学质量验收；未改用户草稿，未自动为用户现有作品发起付费请求。

## OP-20260912-013：API自动恢复与“我”人物线索匹配

- 状态：CHECKED。用户明确不再逐次确认旧请求，授权API自动生成及恢复。
- 实际原因：本地恢复代码直接显示PENDING_REQUEST；最近NPC原始结果含一名用户主角和两名NPC，但线索label=我被要求与姓名或身份字面相等，导致重复schema_error。
- 修复：第一人称线索可匹配唯一用户控制人物，明确名字仍按原规则校验；旧失败原文重新校验成功可复用，保留历史错误，不追加计费。
- 自动恢复保存body/操作ID，不重建输入；网络失败最多追加两次独立记账调用，同进程并发共享结果；不再弹逐次重试确认。
- PASS：117项离线测试、Lint/类型/构建；覆盖恢复不弹确认、第一人称匹配、并发去重、有限重试、重试成功回执复用和旧输出重验。

- 最终隔离浏览器PASS：18次合成调用覆盖有限重试耗尽、无弹窗续试、恢复仅重试摘要、自动开篇和导出。未发起新的真实付费验证，用户原始账本保留。


## OP-20260912-014：减少重复模型请求与流式提前阅读

- 状态：CHECKED。用户采纳生成提速方案并授权实施。
- 诊断依据：此前本机16条含耗时记录的调用中位数99.4秒，7次结构修复累计625.3秒；单次成功NPC曾经历103.3秒初次输出+97.1秒修复。此为优化前历史数据，不作为优化后测速。
- 修改：NPC保留遗漏的既有人物/关系，本地重建关系反向索引，悬空引用与锁定事实仍校验；同进程同故事同内容并发请求共享执行，不同内容提示等待。
- 正常叙事角色资料本地准备，人物演绎与故事线合并一次规划，模型调用5→4。压缩创作方法和编辑/审计元数据，保留原始skill、权威设定、来源和证据，不给正文增加固定字数上限。
- 正文支持SSE流式接收与持久预览；记忆/摘要未完成也可先阅读。预览不进入正式轮次、记忆、导出；刷新保留，完成后只呈现正式正文。步骤计时展示；存储失败不伪装为可重试网络失败。供应商完整JSON响应兼容，LLM_STREAM=false可关闭流式请求。
- PASS：126项离线测试（另3项条件性跳过）、Lint、类型、生产构建、Schema与diff检查；隔离浏览器15次合成调用，验证流式预览、刷新不追加调用、摘要恢复仅1次调用、章节/导出/记忆/多尺寸显示。证据目录：临时shuzhongren-e2e-SWwB9z。
- NOT_RUN：本轮新提示词与流式响应的真实供应商复测、长篇文学质量及实际耗时对比。减少20%调用次数不等于保证减少20%耗时。本轮未新增真实付费调用，未提交/推送。


## OP-20260912-015 · CHECKED · 上下文预算与失败原因

用户采纳方案：保留现有创作快照，按实际提示词、输入及输出预留检查预算；优先精简检索和较旧正文，保留关键事实，超限在模型调用前返回具体原因。任务与前端优先展示轮次真实错误。已有 creativeSnapshot 排除 operations 的改动保留。129项离线测试、Lint、类型、生产构建 PASS；临时隔离回归用当前真实快照验证原开篇恢复可到达故事线模型入口且符合60000字符预算（含8000输出预留），源故事文件逐字未变，未发起付费调用。临时回归测试已移除，常规预算回归留在 request-budget.test.ts。真实生成效果尚未复验。


## OP-20260912-016 · CHECKED · 正文成为首个模型调用

- 新轮次pipeline=direct_v1，人物/故事线/正文一次创作，人物资料和场景提示本地准备；正常每轮4→3次付费调用。content先输出、正文首字时间落盘；记忆和摘要继续在后台任务中完成，保留原子提交及下一轮门禁。
- 旧轮次没有pipeline标记，继续原调用与提示词，复用原回执；备份支持可选流程标记。保留同期OP015场景自然推进及请求预算修复。
- PASS：130项离线测试（4项条件跳过）、Lint、生产构建/类型、Schema、diff；隔离浏览器12次合成请求完成3轮及摘要失败恢复，开篇严格3次调用，正文流式/刷新/导出/备份恢复通过。
- 真实API仅1次短场景测试PASS：deepseek-v4-flash，210字，首字2531ms，完成7564ms，输入63/输出168tokens。隔离记录runtime/performance/latest-latency.json；没有修改用户作品。此数据不代表完整设定长正文或生产全链路时延；未更换模型、未实施按需摘要。
- 本地服务已更新，exec95100，127.0.0.1:3000；重启前现有5个任务均done。本轮没有提交或推送。

## OP-20260913-017：真实小说空白的完整恢复

- 状态：CHECKED。承接用户“检查未生成小说，按已有数据自动生成”的授权。与同期OP015/016共用工作区，保留其直接生成与最终请求预算改动。
- 真实初始会话：story_5fe4e5cf857a4fcdb0541d08de488e59，叙事版本1、零正文；operations去重指纹约3.4万字符被计入上下文造成开篇预算拦截。creativeSnapshot排除根级operations，context.ts使用创作投影计算检索软预算；100000字符运行记录回归覆盖。最终发送仍由同期request-budget.ts检查配置包络，不承诺供应商无限容量。
- 第二个真实阻塞：旁白规划申时洛阳街巷，完整正文推进到黄昏山洞，旧逐字场景相等拒绝保存。移除该错误门禁，保存正文实际结束场景；失败步骤如有成功模型回执则复用、重新本地校验。世界事实与证据规则仍保留。
- 第三个真实阻塞：记忆引文的重复转义与中英文引号差异。新增唯一连续原文匹配的排版纠正，只改引号形式；改写、缺字、模糊或多处匹配仍拒绝。已失败记忆回执本地重验通过，保持原错误、原始输出及尝试记录，调用计数不变。一次性维护测试已删除，通用回归memory-evidence.test.ts保留。
- 运行过程：旧进程未随启动器退出，在替换期间额外执行一次正文调用；自动审批曾要求补充进程父子关系，核实后仅处理本项目服务，未处理其他项目。两份正文回执都保留；成功记忆与正文复用后只继续摘要。本次共6次真实调用：规划59.693秒，正文120.942秒及133.495秒，记忆75.766秒及修复70.717秒，摘要21.619秒；本次多余正文1次，费用单价未知，不能报告免费或具体金额。
- 最终真实结果：原故事原开篇operation_id成功，叙事版本2、正式turns=1、正文5853字符、memory_journal=1、所有步骤done。正文SHA256=035c29e0addb2bea06a2c06e80fa3d4afaded6a20c23e57ad67eced1f9f2f04d。不是预览或合成样例。
- PASS：132项离线测试（4项条件性跳过）、Lint、类型、生产构建。此前本轮隔离浏览器15次合成调用验收PASS，证据shuzhongren-e2e-FKfKLY；同期OP016后续直接流程证据见原事件。最终重启后首页与故事API均200，原故事仍succeeded、5853字符，没有新正文请求。
- 当前本地服务exec5270，http://127.0.0.1:3000。代码未提交或推送；文学质量仍需人工评审，不能用结构通过替代语义质量。


## OP-20260913-018：主角参与NPC对话与剧情停点

- 状态：CHECKED。用户要求正文遇到NPC对主角的对话时暂停，由用户扮演主角回应，对话改变后续故事线，再生成至下一对话。
- 新轮次interactive_v1使用一个正文调用同时识别对话停点；dialogue包含NPC身份和正文末尾原话，无对话时null。主角不得被模型代答或代作关键决定；用户回应逐字保留，NPC身份、台词末尾和回应内容均校验。已知用户角色唯一时自动带入，否则不擅自挑选NPC。
- 停点由最后一段已提交正文派生，回复携带reply_to与叙事版本，过期/未绑定请求在调用前拒绝。幂等指纹、任务、失败恢复、备份均保留绑定。对话UI显示角色、NPC台词、待生成状态、输入及最近对话；刷新不自动回复。
- PASS：135项离线测试、Lint、类型/生产构建、Schema/diff；隔离浏览器18次合成调用通过主角显示、暂停、空回答禁用、草稿刷新零调用、回应进入下一正文及再次暂停。补充回复后摘要失败恢复测试3项PASS，不重复生成已完成正文；备份保留停点。
- 本轮未追加真实模型语义测试；识别细腻对话意图与文学质量仍需真实使用验证，合成测试不代表全部场景。
- 本地服务exec38156，127.0.0.1:3000；重启前7个现有任务均done，017保存的正文和记忆保留。未提交或推送。


## OP-20260913-020 · CHECKED · 故事想法驱动三个模块

发现框架/NPC结果只保存在候选，三个面板仍读取正式草稿；整体生成还附带旧聊天输入。修改use-workbench：框架→NPC串联后自动采用有效组合，NPC前史建议填入世界时间线；局部修改保留候选、锁定/并发编辑校验不变。整体生成不携带旧user_notes，提示词明确最新想法优先与预设非模板。更新hook回归（两次改变想法后正文模块更新、自动填入事件、不中途多调用）。140项测试PASS、4项跳过，Lint/类型/生产构建PASS。未做真实付费语义验证。本工作不改独立NPC对话Agent的实现。


## OP-20260913-021 · CHECKED · 故事想法生成按钮

故事想法下方主按钮明确为“生成”，说明自动填充世界框架、NPC画像与世界时间线；显示生成中状态、禁用重复点击及未恢复输入。主按钮确保整套流程启用NPC，不受旧开关关闭影响。143项测试PASS、5项跳过，Lint/类型/构建PASS。未发起真实生成。本地服务更新后用户刷新即可使用。


## OP-20260913-019：独立NPC对话Agent与可恢复循环

- 状态：CHECKED。用户再次明确正文生成中暂停进入独立多轮对话，对话结束后自动继续正文，持续重复此流程。
- 新pipeline=dialogue_v2在正文问话停点保存transcription后即进入waiting_dialogue并释放锁，尚不调用记忆/摘要。独立弹窗支持多轮NPC消息、收起重开和草稿刷新。每条回答只调用NpcDialogueAgent。
- 对话active→ready→closed；自然结束或明确结束后，确定性汇入完整原文，记忆/摘要后提交，再通过持久continuation_id自动继续正文到下一停点。引入对话版本、逐消息操作回执及模型结果先存后提交；不替用户回答。
- PASS：144项离线测试（5项条件跳过）、Lint/类型/生产构建、Schema/diff；浏览器21次合成调用验证正文一次即弹窗、两轮仅NPC调用、刷新零调用、收起不结束、连续两次自动正文/对话循环。覆盖NPC保存失败、结束后摘要失败、提交后进程中断、成功回放和活动对话备份恢复。
- 真实验收：初验两轮共4次调用（2次非JSON修复），复验两轮2次调用，分别5089/3860ms，均未提前结束。使用独立合成场景，未改用户小说。JSON模式已启用；模型仍补写未提供细节，语义PARTIAL，不冒充全链路通过。
- 记忆保护：对话原话证据按角色陈述落库，不直接提升为旁白事实；原始模型输出、修复与抽取记录保留。证据runtime/dialogue-evaluation/1789230903174、1789231089268；浏览器临时shuzhongren-e2e-C1DIOM。
- 设计与流程图：docs/NPC_DIALOGUE_AGENT.md。未提交或推送。


## OP-20260913-023：设定生成等待优化

- CHECKED：最近真实世界87.136/72.998秒；后一次NPC121.078秒。旧UI等待NPC后才填世界，形成约194秒空白。现在世界校验通过即保存显示，再以已采用版本接续NPC；与当前自动应用改动兼容。
- StoryAgents启用JSON输出模式、移除重复recognition Schema，并要求结构化设定避免重复描述，不缩短正文或删除业务字段。已有严格校验和回执复用保留。
- PASS：147项测试、5项条件跳过；Lint、Schema、生产构建含类型、diff检查；21次合成调用浏览器回归。新增延迟NPC测试验证等待中世界可见、落盘及NPC基于新版本；失败后只重试NPC。浏览器证据：临时shuzhongren-e2e-a7R9u5。
- 未做新真实模型提速复验，不宣称总耗时下降比例。等待实际写作任务结束后更新本项目服务，exec48414、HTTP200；未提交推送。


## OP-20260913-025 · CHECKED · 对话驱动与主角控制

用户采纳互动方案：互动提示词移除完整场景优先，改为短场景尽早到NPC停点，提醒/委托也暂停；仅独处或明确跳过时允许短过渡。增加已知主角对白归属和空对话明显直呼问话校验，错误进入现有单次结构修复；保留用户原话。149项测试PASS、5项跳过，Lint/类型/构建PASS。现有独立多轮窗口复用，旧已完成正文不重写；新轮次生效。规则检测是保守文本校验，不能证明全部语义代言都能识别，真实互动效果待复验。

## OP-20260913-024：默认接受生成改动

- CHECKED：用户要求不再出现“待确认的改动”面板。框架/NPC/revise/field统一自动应用；三方合并保留生成期间的手动编辑，上一份快照仍可撤销。
- 移除主候选面板及NPC页采用入口；旧保存候选在没有在途请求时自动采用，不重新调用模型。无效候选备份后显示具体错误，不留下隐藏候选阻断后续请求。锁定、跨故事与结构校验仍保留。
- PASS：147项完整离线测试（5项条件跳过）；随后新增面板不再渲染的UI回归，8项UI测试PASS。Lint、类型、最终生产构建PASS。涵盖局部自动应用、刷新旧候选零调用、并发编辑保留与撤销。
- 未追加真实模型调用。等待当时运行中的用户开篇结束后才更新服务；该轮自身记忆抽取失败保留检查点，未在本次范围内重新生成。
- 最终服务exec12987。Chrome原标签通过页面重新加载恢复正常，原“少年镖师”输入及“空棺行”设定保留，没有待确认改动面板；程序reload曾被Chrome临时拦截，点击页面自带重新加载后恢复，未修改浏览器保护设置。


## OP-20260913-024：发布main

用户授权提交并推送所有当前项目改动。149项测试、Lint、生产构建/类型、diff检查PASS；密钥排除检查PASS。GitHub HTTPS连接超时；本地main代码提交4cb377a（69文件）。普通push等待35秒超时，fetch无响应已结束，远端未确认更新。操作FAILED，仅网络发布受阻，本地成果完整保留。

OP024发布恢复：再次重试git fetch origin成功，普通git push origin main回执81e8f52..96a39f4，远端main已包含完整代码；此前失败记录保留为历史。工作区干净，未强推、未上传密钥或运行数据。


## OP-20260913-026：本地一键启动脚本

- 新增根目录 `start.py`，可双击或执行 `python start.py`：检查 node_modules（缺失时 npm ci）、启动 `npm run dev`、轮询 `/api/story/status` 直到 HTTP 200、自动打开浏览器；端口被其他程序占用时提示而不误判；Ctrl+C 通过 taskkill /T 结束 npm 与 node 进程树。
- CHECKED：本机 Python 3.12.3 实跑一次成功；Next.js Ready in 504ms，探针 GET /api/story/status 200，页面 GET / 200，浏览器已打开；服务保持运行（exec 64988）。
- 边界：只封装 README 已登记的 npm run dev，未改应用代码、依赖或模型调用；未提交推送。


## OP-20260913-027：演示输入文档

- APPLIED：新增 `examples/demo-wuxia-input.md`，作为演示与测试用的「故事想法」输入。主推版约900字：主角“小虾米”（用户控制、武功低微、在渡口讨生活）、慕容雪（大小姐、剑法与轻功高强、单相思对象）、世界与恩怨留白、开局上元灯会画面、五条边界、四个待定问题；另附短版约280字、可选加戏三句、后续轮次输入示例（NPC 停点回应／续写要求／收束当前章）、预期识别边界与来源合规说明。预设沿用 `eastern_wuxia`。
- CHECKED：文件回读核对通过（CJK、代码块、章节完整）；未发起模型调用，生成质量、耗时与 recognition 实际输出未验证，不作为演示已通过的证据。
- 边界：未改应用代码、Schema、依赖、模型配置与持久配置；`examples/*.json` 仍为 Schema 示例；未提交推送。
- 环境注记：本机 `apply_patch.bat` 包装无法传递多行中文补丁（返回 “The last line of the patch must be '*** End Patch'”），改用 `codex.exe --codex-run-as-apply-patch` 直接传参写入成功；未修改任何工具配置。


## OP-20260913-028：提交并推送 main

- APPLIED：本地提交 066ecf23“feat: add one-click start script and wuxia demo input”，包含 start.py、examples/demo-wuxia-input.md（新增）、README.md、memory/CURRENT.md、memory/HISTORY.md，共 5 文件、236 插入、4 删除。
- CHECKED：`git push origin main` 回执 3cdb2b3d..066ecf23；`git ls-remote origin refs/heads/main` 返回 066ecf23eefab19f892fe5bfdc0876f41066970f，与本地 `git rev-parse HEAD` 一致。未强推、未改全局配置，暂存内容不含 .env*、runtime/、output/、.next。
- 边界：next-env.d.ts 的 `.next/dev/types` 自动改写、`0913 存储/0913 存储.zip`（约490MB）与根目录 8 个 `modify_*.py` 临时脚本未纳入提交；纳入或删除需用户明确指示。
- 未变边界：PRODUCT-04 真实长篇语义验收仍 PARTIAL，PRODUCT-06 公网发布仍 TODO，分别需要真实模型费用与目标环境。


## OP-20260913-029：演示输入真实验收（阻塞）

- APPLIED：本地服务恢复。原 3000 端口拒绝连接；以 `npm run dev` 重启，Ready in 332ms，`GET /api/story/status` 200，模型 `doubao-seed-2-0-pro-260215`，账本 used=24，exec session 68084。
- APPLIED：新增 Git 忽略的一次性验收 harness `runtime/dev/vitest.demo.config.ts`、`runtime/dev/demo-wuxia.test.ts`、`runtime/dev/run-demo.mjs`，读取 `examples/demo-wuxia-input.md` 主推版（2563 字节）并调用真实 `StoryAgents.framework`、`npcs` 与 `MainAgent.turn`，工件写入 `runtime/dev/demo-wuxia-<时间戳>/`。
- FAILED（外部阻塞）：`framework` 首次真实调用即返回 `httpStatus 401`、`provider_error`、402ms，证据 `runtime/dev/demo-wuxia-2026-09-13T01-12-13.778Z/ledger.json`。`runtime/model-requests.json` 最近成功调用为 2026-09-12T09:47Z（http 200，118477ms），其后多次失败均已保留；`LLM_BASE_URL` 指向 api.openai-next.com，判断为密钥失效或被拒，非应用缺陷。未重复重试，未产生成功模型输出。
- 环境注记：首次运行因 test 环境不加载 `.env.local` 报“未配置模型”（工件 `runtime/dev/demo-wuxia-2026-09-13T01-12-04.413Z/report.json`）；按 `scripts/testing/real.mjs` 先在父进程 `loadEnvConfig` 再派生 vitest 后解决，该记录不代表产品缺少模型配置。
- 下一步：用户提供有效 `LLM_API_KEY` 后可重跑 `node runtime/dev/run-demo.mjs`，或在本地 UI 粘贴主推版走演示；演示是否通过仍为 NOT_RUN。



## OP-20260913-030：极简主对话入口

- 新增默认「对话」页（ConversationHome）：一条消息流加一个输入框。无会话时提交想法即依次执行 framework → NPC → initialize → 开篇轮次；有会话时同一输入框走既有叙事轮次与 NPC 对话停点；世界、人物、时间线只显示摘要卡片，点击进入设定工作台。
- 抽出 NarrativeProse（正文与流式预览）与 NarrativeCompose（进度、恢复、可能计费操作、输入），对话页与写作台共用；写作台保留正文 / 人物与记忆 / 章节与伏笔 / 设定同步四个面板；导航改为对话 / 故事设定 / 写作台 / 我的作品。
- 修正过期请求文案：revision_conflict 由“草稿已修改，请基于当前内容重新生成。”改为“上一轮生成已过期：草稿在请求之后有改动。基于当前草稿重新生成会再次调用模型。”；错误横幅重试按钮改为“按当前草稿重新生成”，并同步更新对应 UI 测试。
- 验证 PASS：156 项离线测试（新增 ConversationHome 5 项、WritingApp 集成 2 项）；Lint 无问题；tsc 对本次改动文件无错误；真实浏览器（独立 playwright 会话、空存储）确认默认页为对话页、导航可切换、空输入时按钮禁用，点击“开始故事”确实发出 POST /api/story/framework。
- 边界：合成模型 scripts/testing/model-server.mjs 不含 framework 与 NPC 画像分支，无法用它跑“一句话开始”全链路，该链路改由 jsdom 集成测试覆盖；生产构建被 runtime/dev/demo-wuxia.test.ts（另一会话的 Git 忽略 harness）类型错误拦住，webpack 编译本身 PASS。该 framework 真实调用在本次验证中同样失败（账本 used 24→25），与 OP-20260913-029 记录的 401 一致。
- 未提交、未推送。


## OP-20260913-031：等待标记与 C 端文案

- 新增等待／演化标记 GenerationStatus：脉冲圆点 + 当前阶段 + 已等待秒数 + 逐步状态 chips；对话页在世界与人物生成、叙事演化两种情况下都显示，进度文案与步骤标签抽到 step-labels.ts 供对话页、写作台共用；CSS 动画在 prefers-reduced-motion 下关闭。
- C 端文案统一：对话页、写作台、我的作品、记忆面板、角色对话窗口、主 Agent 阶段提示与主要服务端提示改为用户语言。去术语（NPC→角色、主 Agent→书中人、快照/校验/计费→存档/检查/额度），关键动作更直白（推进故事→继续写，续写要求（可留空）→接下来写什么（可留空），恢复未完成轮次（可能计费）→接着写上次没写完的（会消耗额度），生成遇到问题→按当前草稿重新生成）。同步更新单元测试与 scripts/testing/writing.playwright 断言。
- 真实模型状态核实：.env.local 指向 api.openai-next.com，模型 doubao-seed-2-0-pro-260215，密钥已配置（51 字符）；/api/story/status 返回 configured=true。但 9/13 的两次真实调用均 HTTP 401（09:19、09:28，约 0.35 秒），最近成功记录为 9/12 17:28–17:47；未配置单价，费用无法估算。判断为密钥失效或被拒，非应用缺陷。
- PASS：158 项离线测试（本轮新增 2 项）、Lint、tsc 对本次改动文件无错误。未提交、未推送。


## OP-20260913-032：快模型开关与供应商 401 核实

- 新增可选快模型：readModelConfig(env, role) 支持 LLM_MODEL_FAST；世界观/人物等设定生成（StoryAgents）与角色对话（npcDialogue）走 fast，正文、记忆、摘要、章节仍走 LLM_MODEL。JSON 客户端新增 options.fast，账本按实际模型记录。.env.example 与 README 模型表已登记。
- 供应商核实：用 .env.local 的密钥请求 GET https://api.openai-next.com/v1/models 返回 401 Unauthorized，说明密钥在账号级被拒，而非模型名或应用缺陷；因此无法列出可用模型、也无法真实生成。历史成功记录为 9/12 17:28–17:47，9/13 两次调用均为 401（约 0.35 秒）。
- PASS：158 项离线测试（含 fast 角色选择与图像模型拒绝 3 条断言）、Lint、本次改动文件 tsc 检查。未把 flash 模型名写入 .env.local（等待用户提供可用模型名或有效密钥）。未提交、未推送。


## OP-20260913-032：flash 模型接入与世界演化闭环

- flash 接入：.env.local 增加 LLM_MODEL_FAST=deepseek-v4-flash-vision-exp。代码侧接线原本已在：readModelConfig(env,"fast")、设定类 Agent 默认走 fast、角色对话 fast:true；正文/记忆/摘要仍走 LLM_MODEL。新增 src/lib/ai/model-config.test.ts 锁定 fast 角色解析、空值回退与状态接口仍显示主模型；服务已重启加载新环境变量。真实可用性未验证（当前密钥 401）。
- 世界演化闭环：每轮叙事写完后，客户端用快速模型（revise target=timeline）把新出现的地点/事件/势力补进世界年表，结果按现有自动应用流程进入工作台设定并保留来源；对话页出现「世界观已经有新的演化（第 N 版），并入故事后下一轮写作就会用上」与一键「并入故事」，点击后 sync 进该故事的叙事会话，下一轮上下文即包含它。每轮只触发一次且记录在 localStorage（shuzhongren.evolved.<storyId>），刷新不重复计费；纯角色对话轮不触发。
- 验证：lint 干净、161 项离线测试通过、tsc 对本次改动文件无错误。世界演化链路尚无自动化测试，且未在真实模型下跑通（密钥 401）。
- 未提交、未推送。


## OP-20260913-033：真实密钥接入与模型探测

- 用户提供新的 LLM_API_KEY（api.openai-next.com）。已写入 .env.local（该文件被 gitignore，未写入文档、日志或提交），服务已重启加载。
- 新增 tests/model-probe.test.ts：默认跳过，RUN_REAL_MODEL=1 时对 text 与 fast 两个角色各发一次最小请求。实测两条均返回内容（约 5 秒内完成）→ 密钥有效，且 deepseek-v4-flash-vision-exp 在中转可用；此前 401 的历史不再复现。
- 该探测直接使用 ChatCompletionsClient，不写请求账本；真实业务调用仍走账本与幂等回执。
- 门禁：lint 干净、161 项离线测试通过（6 项条件跳过，含本探测）、/api/story/status configured=true。
- 下一步：用真实模型跑一句话→世界观/人物/开篇、一轮后的世界演化（年表增长 + 并入故事）、多轮角色对话（走 flash）。未提交、未推送。


## OP-20260913-034：真实链路首轮实测与两处修复

- 密钥生效：新 LLM_API_KEY 写入 .env.local 后，真实探测（tests/model-probe.test.ts，RUN_REAL_MODEL=1）显示 text 与 fast 两个角色都能返回内容；401 不再复现。
- 真实首轮实测（主推版武侠输入 877 字，经对话页粘贴并提交）：账本确认 framework 调用确实打到 deepseek-v4-flash-vision-exp（HTTP 200，42.6 秒），但回执为 incomplete_output（输出为空或被过滤）→ flash 不适合结构化世界观生成。已把设定类 Agent 改回主模型（src/lib/ai/agents.ts），flash 仅保留在角色对话等短交互。
- 供应商侧随后阻断：主模型两次真实调用分别 HTTP 429（限流，45.5 秒）与 HTTP 524（网关超时，125.2 秒，回执 uncertain）。首轮验收未完成，结论 NOT_RUN，账本 used 26→29。
- 修复：run() 的失败分支在服务端已给出明确错误时清空 pending（此前失败后 pending 仍保留，导致每次刷新页面自动重发并再次计费——27→29 两次调用即由此产生）。这也是一部分 revision_conflict 提示的成因。
- 门禁：lint 干净、161 项离线测试通过（6 项条件跳过）。未提交、未推送。


## OP-20260913-035：flash 作为主模型的实测结论

- 按用户要求把 LLM_MODEL 与 LLM_MODEL_FAST 都设为 deepseek-v4-flash-vision-exp，并新增 LLM_NO_RESPONSE_FORMAT_MODELS 规避 response_format（该模型收到 response_format 会返回空内容）。真实探测三条全过（含小 JSON，约 15 秒三次调用），确认 base URL 用 https://api.openai-next.com/v1。
- 但用同一模型跑真实完整世界观生成两次均失败：HTTP 200、42.6 秒与 41.4 秒、回执 incomplete_output（空内容或被过滤）。同一请求在小 JSON 场景正常 → 判断为该模型在长结构化输出上把预算消耗在推理、最终没有内容（vision-exp 变体），不是应用缺陷。
- 处理：LLM_MODEL 恢复为 doubao-seed-2-0-pro-260215（已验证可产出合法结构化输出，15.8 秒），flash 保留在 fast 角色并继续使用 response_format 规避名单。门禁：lint 干净、162 项测试通过（8 项条件跳过）。
- 未完成：一句话→世界观/人物/开篇、世界演化、多轮对话的真实链路验收（卡在第一步）。建议下一步先给 flash 提高或去掉 max_tokens（或换非 vision 的 flash）再对照；探测脚本 tests/model-probe.test.ts 可直接复用。