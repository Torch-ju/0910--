# CURRENT：唯一当前开发进度

## 当前工作单元

- operation_id / last_operation_id：OP-20260913-029。
- 目标：恢复本地服务，并用真实模型对演示输入做一次有界验收（世界框架+识别+NPC+首轮）。
- 操作状态：BLOCKED（服务已恢复；真实模型返回 401，验收未完成）。
- 用户授权：实施本地功能及真实文本模型验收；公开部署仍需目标环境，用户已明确授权将全部代码和文件推送到仓库主分支。
- 影响文件：runtime/dev/（本地验收脚本与证据，Git 忽略）、memory/HISTORY.md、memory/CURRENT.md。

## 本轮交付与验证

| 任务 | 状态 | 证据与边界 |
| --- | --- | --- |
| PRODUCT-01 本地写作流程 | DONE | 写作入口、真实步骤、任务状态、刷新与失败恢复；合成浏览器完整流程 PASS |
| PRODUCT-02 作品与记忆管理 | DONE | 列表、章节、Markdown、完整备份、版本同步、身份合并/拆分、事实冲突选择、放弃轮次 |
| PRODUCT-03 本地可靠性 | DONE | 上下文字符预算、相关记忆检索、检查点、响应后任务、步骤边界取消、死进程锁回收、账本成功回执恢复；离线验证通过；单进程本地范围，长篇与多进程压力未验收 |
| PRODUCT-04 真实模型验收 | PARTIAL | 两种世界/NPC通过；武侠3轮、魔幻2轮完整成功。魔幻第3轮记忆网络中断；人工语义问题见 docs/LOCAL_ACCEPTANCE.md |
| PRODUCT-05 工程交付 | DONE | 仓库内 PRD / MAIN_AGENT_RULES；可重复 test:e2e PASS；干净 npm ci、测试与构建 PASS；最终108项离线测试及构建通过；验收报告已保存 |
| PRODUCT-06 公网发布与 Git 推送 | TODO | 本地可用不以发布为前提；目标环境、账户、数据库与部署未实施；未提交或推送 |

- 模型配置实际填在 .env.example，已迁移到被忽略的 .env.local；示例和临时干净副本已清空密钥值。
- 不将离线/合成结果视为真实文学质量验收。真实报告与失败原文留在被 Git 忽略的 runtime/evaluation。
- 当前源码与新文档已提交至本地 dbe83b7，远端推送未确认；用户原有 transcription-agent.zip 删除保留，展开目录作为源码交付。
- 最新浏览器命令使用 /private 临时故事与合成模型，11 次合成调用；刷新无新调用，摘要恢复仅新增 1 次。375/768/1280/1440 宽度无横向溢出。
- 浏览器原生 download 事件一次超时，已改为验证导出 Markdown 内容；不宣称验证了操作系统下载落盘。
- 正式权威产品文档现位于仓库 PRD.md / MAIN_AGENT_RULES.md，上级同名文档为同步副本。

## 当前证据与后续边界

- 完整证据：docs/LOCAL_ACCEPTANCE.md；可重复协议：docs/REAL_MODEL_ACCEPTANCE.md。
- 最终 Lint、类型、Schema、108 项测试、生产构建、diff 检查 PASS；独立记忆模块12项 PASS。
- 后续优先解决来源标记在画像/事件展示中丢失、用户控制扩写和不充分证据推断，再进行魔幻第三轮及真实长篇验收。
- 不再自动追加本轮付费复验；失败步骤和全部历史已保存，可显式恢复。
- 公网、Git 发布和真实长篇仍未完成；旧第一层记录已归档至 HISTORY，不作为当前运行状态。

## 最新修复 OP-20260912-008

- 世界框架生成来源误判已修复；本机 Host 与 Next 内部地址差异不再误拦截。
- 边界测试2项、Lint、构建及运行HTTP检查PASS，未发起模型调用。
- 本地服务已重启，http://127.0.0.1:3000；用户刷新后可重新生成。此前真实语义与公网边界不变。

## 发布操作 OP-20260912-009

- 用户已授权推送 origin/main；全部代码及交付文档已整理，109项离线测试通过。
- 敏感配置、运行数据与系统缓存排除；GitHub:443 连接超时，普通推送未获成功回执；网络恢复后先fetch核对再push。

## 最新画像优化 OP-20260912-010

- 世界成功但NPC未发起，生成结果中的两个关键问题阻塞后续。已补人物页就地回答、条件说明和候选采用入口。
- 111项离线测试与隔离浏览器全流程PASS；无需重复生成世界，刷新后进入NPC画像处理问题即可。
- 本轮代码尚未提交；此前推送网络阻塞记录仍保留。真实模型语义与公网边界不变。

## 最新自动生成规则 OP-20260912-011

- 当前规则替代OP010的画像候选阻塞：输入→世界/历史开局时间线→自动NPC→组合候选采用。缺失信息给建议，不要求先回答。
- 112项测试、Lint/类型/构建、隔离浏览器PASS；真实语义效果未新增复验。
- 网站已更新，本轮代码尚未提交；此前Git推送仍受网络影响。

## 最新写作体验 OP-20260912-012

- 开始写作无需逐项确认/回答问题，当前有效候选可直接使用，自动开篇；留空输入可续写。
- 项目接入指定jin-yong-perspective创作方法，正文取消固定字数校验，模型实际容量仍受服务限制。
- 113项离线测试、Lint/类型/构建及浏览器16次合成调用PASS；新真实文学质量尚未验收。
- OP011遗漏的服务端NPC阻塞已修复。代码尚未提交，原Git网络发布状态不变。

## 当前修复OP013

用户授权API自动生成和恢复，不再逐次确认重试。真实NPC账本失败为“我”未覆盖，修正第一人称匹配；恢复旧请求并有限重试。

- OP013验证完成：117项测试、Lint/类型/构建及18次合成调用浏览器流程PASS。已移除旧pending确认提示；刷新自动恢复原请求，第一人称匹配已修正，旧结果可重验复用。

## 当前提速OP014

用户采纳优化方案；实施结构索引本地修复、已有角色保留、合并人物/故事线规划、正文流式预览和输入压缩。


- OP014验证完成：126项离线测试、Lint/类型/Schema/构建、隔离浏览器15次合成调用PASS。正常每轮4次模型调用，正文提前展示，摘要失败只恢复摘要；未追加真实供应商测速，实际速度与文学质量仍待实测。
- 下一项具体动作：用户刷新本地网站验证真实生成体验；结合新步骤计时比较人物/故事线、正文和记忆摘要的耗时，再决定是否调整供应商。不要把合成测试耗时当作真实提速数据。

- 本地提速版本已启动：127.0.0.1:3000，exec会话61756。重启前3个现有故事任务均为done；仅停止本项目旧进程，其他项目进程保留。


## OP-20260912-015 · CHECKED

上下文按实际请求检查，保留输出预留（NARRATIVE_OUTPUT_RESERVE_CHARS 默认8000），去掉操作日志，自动精简可选历史并保留最近场景尾段；关键设定超限明确报错。129项测试、Lint、类型、构建PASS，当前用户故事隔离回归PASS且未修改源故事/调用付费模型。下一步：重启本地新构建后恢复现有开篇，真实文学质量与耗时待验证。

- OP015本地服务已重启：会话72734，HTTP首页200；未自动恢复付费生成，用户可从现有失败轮次恢复。

## OP-20260912-016 · CHECKED

用户要求进一步提速。新轮次直接规划并生成正文，保留旧轮次恢复契约；后台整理与流式预览继续，验证首个付费请求就是正文。


- OP016已交付：新轮次第一调用直接创作正文，后台记忆与摘要，正常每轮3次调用。130项离线测试、构建/类型/Lint/Schema和隔离浏览器12次合成调用PASS。
- 当前API短场景真实测速：首字2.531秒、210字完成7.564秒；不能外推为长正文/完整设定耗时。仅1次真实调用，记录runtime/performance/latest-latency.json。
- 当前服务：127.0.0.1:3000，exec95100。下一项具体动作：用户刷新后新轮次使用direct_v1，观察完整正文首字与完成时间；旧未完成轮次沿原流程恢复，避免重新支付已成功步骤。按需摘要、更换模型未实施。

## OP-20260913-017 · CHECKED

真实开篇三处阻塞已修复：运行指纹进入上下文、开场/结束场景逐字相等、记忆引号排版差异。原“侠之大者”已正式保存5853字符，版本2、1轮正文、记忆/摘要/章节检查全部done；刷新页面即可阅读。原模型回执与失败证据保留。

本次6次真实调用包含旧进程额外正文1次与记忆结构修复1次；本地重验记忆零新增调用，后续没有第三次正文生成。132项离线测试、Lint/类型/构建通过；最终HTTP首页和故事API均200。服务exec5270，127.0.0.1:3000。同期OP016新轮次直接生成规则保留。

下一项具体动作：用户阅读开篇并续写，评审长期人物/时间线一致性及正文质量；无需再恢复本次已完成任务，不重新发起开篇。尚未提交/推送。本轮详细真实证据与费用边界见HISTORY同ID。


## OP-20260913-018 · CHECKED

- 主角对话循环已实现：新轮次interactive_v1，NPC问话停点→用户回应→正文后果→下一停点。旧正文/失败流程保留，不回溯拆分已生成内容。
- 135项离线测试、Lint/类型/构建/Schema、隔离浏览器18次合成调用PASS；回复后的摘要恢复不重复生成正文，备份与刷新保留停点和回复。
- 本轮未做真实模型对话语义复验。下一具体动作：用户刷新网站，在下一新剧情中观察NPC提问后以主角身份回应；评估实际人物代入、对话识别及故事线影响。
- 当前服务127.0.0.1:3000，exec38156；017已完成的真实正文与记忆未修改，重启前7个任务均done。

## OP-20260913-019 · CHECKED

独立NPC对话Agent：正文停点立即保存并释放故事锁，在记忆摘要之前进入多轮对话；结束后整理对话并自动续写至下一停点。覆盖刷新、重复请求、恢复和备份。


## OP-20260913-020 · CHECKED

三个设定模块改为按当前故事想法生成后自动填入草稿，不再停在候选区；已有锁定/编辑冲突保护保留。140项测试、Lint/类型/构建PASS；两次输入变化回归验证完成，真实模型内容质量未复验。OP019独立对话工作状态按其原记录保留，不在此宣称完成。下一步：用户刷新后点击整套生成，检查实际输入细节覆盖。


## OP-20260913-021 · CHECKED · 故事想法生成按钮

故事想法下方主按钮明确为“生成”，说明自动填充世界框架、NPC画像与世界时间线；显示生成中状态、禁用重复点击及未恢复输入。主按钮确保整套流程启用NPC，不受旧开关关闭影响。143项测试PASS、5项跳过，Lint/类型/构建PASS。未发起真实生成。本地服务更新后用户刷新即可使用。


- OP019已交付：dialogue_v2独立多轮对话，在记忆摘要前弹窗；结束后自动续写，再次触发对话。设计/流程图docs/NPC_DIALOGUE_AGENT.md。
- 144项测试及浏览器21次合成调用PASS，含跨阶段中断、存储失败和活动对话备份。真实NPC复验两轮均单次返回，约5.1/3.9秒；知识边界仍有扩写，语义PARTIAL。对话原话证据降为角色陈述保护。
- 下一具体动作：用户刷新后从下一新轮次体验独立窗口与多轮交流；评审NPC是否保持角色知识、对话自然结束及续篇方向。不要用合成流程结果替代真实长篇语义验收。

- OP019本地服务已更新：127.0.0.1:3000，exec49261；重启前无运行中的任务，健康检查HTTP200。


## OP-20260913-023 · CHECKED · 设定生成提速

FR-02/FR-09：真实世界调用87.136秒、72.998秒，后一NPC121.078秒。世界立即采用和保存后再补NPC，避免整套194秒结束才显示世界；与正在修改的自动应用流程兼容。设定调用启用JSON模式，去掉重复recognition Schema，减少字段间重复描述，正文长度不变。延迟NPC测试验证等待中可见/已保存/只恢复NPC，NPC失败保留世界。当前147项测试PASS、5项条件跳过；生产构建、Lint、Schema和21次合成调用浏览器回归PASS，尚未做新模型速度复验。已确认无运行中的故事任务后更新本地服务，exec48414，HTTP200；用户刷新即可使用。

## OP-20260913-024 · CHECKED · 默认接受改动

用户要求取消待确认改动面板。所有生成入口自动应用，旧候选刷新后自动合并，保留并发用户编辑与撤销；校验失败备份结果而不留下不可见候选门禁。147项全套测试与新增UI回归通过；Lint/类型/最终构建PASS。已更新服务exec12987并验证Chrome原故事数据保留、没有确认面板。与OP023世界先显示流程兼容。本轮未追加模型调用；另一开篇记忆失败检查点保留，后续单独处理。


## OP-20260913-025 · CHECKED · 对话驱动与主角控制

用户采纳互动方案：互动提示词移除完整场景优先，改为短场景尽早到NPC停点，提醒/委托也暂停；仅独处或明确跳过时允许短过渡。增加已知主角对白归属和空对话明显直呼问话校验，错误进入现有单次结构修复；保留用户原话。149项测试PASS、5项跳过，Lint/类型/构建PASS。现有独立多轮窗口复用，旧已完成正文不重写；新轮次生效。规则检测是保守文本校验，不能证明全部语义代言都能识别，真实互动效果待复验。


## OP-20260913-024 · FAILED · 发布main

用户明确授权将当前全部项目改动推送GitHub main。149项测试、Lint、生产构建含类型及diff检查PASS，5项条件测试跳过。配置密钥扫描无命中；.env.local、runtime和构建产物不纳入提交。GitHub443连通性10秒超时，正在完成本地提交并尝试普通推送，不强制覆盖远端。

OP024结果：代码已提交本地main，4cb377a（69文件）。普通git push origin main等待35秒超时，无成功回执；GitHub443诊断10秒连接超时，fetch无响应已结束。未强推。下一步网络恢复后先fetch核对远端，再普通push；不要声称远端已更新。


OP024后续核验：再次重试时直连fetch与push已成功，GitHub main更新至96a39f4，代码提交4cb377a已包含。此前超时为历史状态；同时只读发现系统代理127.0.0.1:7890，可按请求通过git -c http.proxy使用，未改全局配置。不能把代理认定为本次直连恢复的已证实原因。


## OP-20260913-026 · CHECKED · 本地一键启动脚本

新增根目录 `start.py`（双击或 `python start.py`）：检查依赖、启动 `npm run dev`、等待 `/api/story/status` 返回200、自动打开浏览器；端口被占用时区分“已在运行”与其他程序；Ctrl+C 结束 npm 与 node 进程树。本机 Python 3.12.3 实跑一次成功：Next.js Ready in 504ms、探针200、页面200，浏览器已打开，服务保持运行（exec 64988）。下一步：用户可直接双击 start.py 启动；应用功能与真实模型验收边界不变。


## OP-20260913-027 · CHECKED · 演示输入文档

新增 `examples/demo-wuxia-input.md`：主推版（约900字）、短版（约280字）、可选加戏三句、后续轮次输入示例（NPC 停点回应／续写要求／收束当前章）、预期识别边界与来源合规说明。输入对齐既有契约：主角由用户控制、慕容雪作为 NPC 人物线索、五条边界对应内容边界，末尾留白对应待确认问题；预设沿用 `eastern_wuxia`。CHECKED：文件已回读核对，CJK 与代码块完整；未发起模型调用，生成质量、耗时与 recognition 实际输出均未验证，不混同为演示通过。未改应用代码、Schema、依赖与模型配置；`examples/*.json` 仍是 Schema 示例。下一步：用户刷新网站后按文档步骤粘贴主推版走一遍演示（如需真实复验会产生模型费用）；真实长篇语义验收与公网发布等待办不变。


## OP-20260913-028 · CHECKED · 提交并推送 main

本地提交 066ecf23（5 文件、236 插入）：start.py、examples/demo-wuxia-input.md（新增）、README.md、memory/CURRENT.md、memory/HISTORY.md。`git push origin main` 回执 3cdb2b3d..066ecf23；`git ls-remote origin refs/heads/main` 返回 066ecf23eefab19f892fe5bfdc0876f41066970f，与本地 HEAD 一致，远端已更新。未强推、未改全局配置，未包含 .env*、runtime/、output/ 与 .next。

未纳入本次提交：next-env.d.ts（Next 开发模式自动改写为 .next/dev/types，属生成噪声）；`0913 存储/0913 存储.zip`（约490MB 备份包，不适合入仓）；根目录 8 个 `modify_*.py` 临时改写脚本（历史工具脚本，记忆与文档均未引用）。纳入或清理需用户明确指示，删除仍按审批处理。

下一步：用户在本地按 `examples/demo-wuxia-input.md` 的步骤粘贴主推版走一遍演示；PRODUCT-04 真实长篇语义验收（PARTIAL）与 PRODUCT-06 公网发布（TODO）分别需要真实模型费用与目标环境。


## OP-20260913-029 · BLOCKED · 演示输入真实验收

本地服务原已停止（3000 拒绝连接）；重新以登记的 `npm run dev` 启动，Next.js Ready in 332ms，`GET /api/story/status` 返回 200，模型 `doubao-seed-2-0-pro-260215`，账本 used=24（exec session 68084）。服务现已可用，用户可直接演示。

验收脚本 `runtime/dev/{vitest.demo.config.ts,demo-wuxia.test.ts,run-demo.mjs}`（Git 忽略的一次性验收harness，按 `scripts/testing/real.mjs` 方式先注入 `.env.local`）取 `examples/demo-wuxia-input.md` 主推版，走 `StoryAgents.framework → npcs → MainAgent.turn`。

BLOCKED：真实调用返回 `httpStatus 401`（402ms，`provider_error`，证据 `runtime/dev/demo-wuxia-2026-09-13T01-12-13.778Z/ledger.json`）。`runtime/model-requests.json` 中最后一次成功调用为 2026-09-12T09:47Z，此后均为失败；`LLM_BASE_URL` 为 api.openai-next.com。判断为密钥失效/被拒，不是应用缺陷；修复需用户提供有效 `LLM_API_KEY`。按“同一问题不盲试”要求未重复重试。

同时说明：`runtime/dev/demo-wuxia-2026-09-13T01-12-04.413Z/report.json` 记录的是 harness 环境未注入 `.env.local`（test 环境不加载 `.env.local`）导致的“未配置模型”，属脚本环境问题，不代表产品配置缺失。验收结论为 NOT_RUN，不得写成演示已通过。
