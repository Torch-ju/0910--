# 书中人：多 Agent 小说共创产品需求文档

- 文档版本：2.0
- 日期：2026-09-12
- 范围：从故事设定、逐轮演绎到小说转写、人物记忆、摘要与章节归档。
- 配套执行契约：[主 Agent 调用规则](MAIN_AGENT_RULES.md)。
- 项目目录：[0910--](0910--)。
- 本文定义产品要求，不以需求文字代替运行验证；实际实现与验证记录见 [CURRENT](0910--/memory/CURRENT.md)。

## 1. 背景、依据与资料冲突处理

现有项目已具备世界框架、人物画像、世界初始时间线及字段共创能力。独立人物记忆模块具备事件、事实、关系、身份纠错与画像更新能力；转写设计包规定将角色和旁白输出转换为小说正文。用户本次要求把这些能力整合为完整产品，并由一个主 Agent 统一调用。

本 PRD 采用以下依据：

| 资料 | 本文采用的内容 | 边界 |
|---|---|---|
| `0910--/docs/PRODUCT_SPEC.md` | 世界/人物事实包装、用户确认、锁定、版本、时间线、第一层界面 | 继续作为第一层详细规范；完整范围由本文扩展 |
| `0910--/ARCHITECTURE.md` | 主叙事、章节、摘要、上下文持续累积的总体方向 | 旧项目路径和未实现技术栈不视为现有能力 |
| `0910--/src` | Next.js 工作台、四类设定 Agent、模型请求账本、候选合并 | 以源码核实实际行为 |
| `0910--/memory-agent` | 人物记忆、证据、身份、关系、检索、纠错通知、100 字画像 | 动态画像与初始人物设定需要适配，不直接互相覆盖 |
| `0910--/transcription-agent` | 转写职责、素材优先级、正文输出、章节归档边界 | 修正输入示例与 Schema 的类型冲突；不依赖他人机器上的 Skill 路径 |
| 三份通用技术/前端/部署手册 | 类型校验、真实与模拟验收分离、状态恢复、隐私、发布验证 | 默认栈可适配；不强制把已有 Next.js 后端重写为 Python |
| 参赛手册 PDF | 强调用户数据累积、可解释结果、演示和提交材料 | 文档列有魔搭创空间部署要求；日期、提交时间和平台要求发布前需向赛事方核实 |

`0910--/提交/产品规格.md` 实际描述活动报名系统，不作为书中人的需求依据。开发记忆中的历史模型额度、旧运行地址、旧测试结果不作为当前事实。当前用户明确要求优先于旧的“仅限第一层”范围约束。

## 2. 产品定位与目标用户

书中人是让用户保留作者与角色控制权的中文小说共创工作台。用户先与 AI 建立可信的故事世界和人物，再以逐轮行动、对白或叙事指令推进故事。系统持续保存已经发生的内容、人物变化和未解伏笔，减少长篇创作中的失忆、设定漂移与重复劳动。

目标用户：

- 有故事想法但难以建立世界与人物的新手作者。
- 希望以角色互动方式探索剧情的创作者。
- 需要维护连续人物状态、关系和伏笔的连载作者。

核心价值：让作者能看到、修改并追溯 AI 使用的设定与记忆；每轮输出形成可以继续写作和归档的原创正文。

## 3. 产品目标、非目标与成功指标

### 3.1 产品目标

1. 从自由描述建立可编辑、可确认的世界与人物。
2. 保留用户控制人物的关键选择，不由模型替用户决定。
3. 把一次有效剧情输入转换为一段与前文相容的小说正文。
4. 将人物记忆、剧情摘要和章节归档纳入同一流程。
5. 失败时保留输入和已完成步骤；重复请求不重复提交章节、记忆或剧情。
6. 向用户清楚区分待确认设定、已发生事件、推断、传闻和未来伏笔。

### 3.2 非目标

本期不要求多人协作、商业账号与计费、公开作品社区、音视频生成、自动发布平台、模仿或复制既有作品。通用闲聊不是默认写作动作；角色画像生成与角色演绎是不同职责。

### 3.3 验收与观测指标

| 指标 | 定义与验收口径 |
|---|---|
| 单轮完成率 | 已成功提交的轮次数 / 有效发起轮次数；必须区分真实模型与模拟数据 |
| 恢复正确性 | 同一操作重复提交只增加一次叙事版本、一次记忆记录；自动化用例必须通过 |
| 设定保护 | 锁定内容不被字段或文档候选覆盖；未确认设定不能进入正式叙事初始化 |
| 记忆可追溯 | 每条抽取证据都能定位本轮正文；人物 ID 可与初始设定对应 |
| 章节一致性 | 章节正文由已提交片段组成，source_turn_ids 可定位每一片段 |
| 可用性 | 世界设定和后续写作核心操作可在桌面和移动端完成；新增界面须真实浏览器验收 |
| 成本与延迟 | 按模型步骤记录请求、修复、失败、token 与耗时；不捏造 token 或进度百分比 |
| 文学与逻辑质量 | 使用武侠/魔幻、多轮状态变化、身份伪装等固定案例进行人工评审；尚无真实样本时不承诺数值准确率 |

## 4. 用户旅程

### 4.1 建立世界与人物

进入工作台 → 选择东方武侠或西方魔幻 → 描述故事想法 → 自动生成世界及历史/开局时间线 → 接续识别并生成 NPC → 采用组合候选 → 编辑/澄清人物与关系 → 确认有效设定 → 初始化写作会话。

采用候选不等于批量确认所有事实。用户可以拒绝建议或锁定字段；未解决的关键问题不阻止进入叙事阶段，作为创作参考保留。

### 4.2 逐轮写作

读取服务端最新会话 → 用户输入角色行动、对白或叙事意图 → 主 Agent 保存输入 → 本地人物资料 → 人物与故事线统筹 → 小说转写 → 记忆抽取与投影 → 更新摘要 → 判断章节归档 → 一次提交正文、记忆和摘要 → 展示结果及通知。正文生成过程中可提前阅读标明“尚未完成整轮保存”的预览。

用户行动意图不自动意味着行动成功。例如“我一剑击败所有人”应由既有能力与场景约束裁定，不能凭用户描述直接创造超出设定的能力。

### 4.3 异常恢复

某一步失败 → 展示失败阶段及已保留材料 → 查询最新状态 → 同操作 ID 请求恢复 → 已成功步骤直接复用 → 需新模型调用时由用户明确重试 → 完整成功后只提交一次。

刷新或断网不自动重新付费调用。停止浏览器等待不等于取消模型任务。

## 5. Agent 角色与职责

整合后的目标结构为 **1 个主 Agent + 10 个子职责**。子职责不要求各自独立部署，也不意味着每轮调用十次模型。

| ID | Agent / 子职责 | 负责 | 禁止 |
|---|---|---|---|
| A00 | 主 Agent | 意图路由、前置检查、上下文、顺序、重试、提交、状态查询 | 随意跳过依赖或把未验证结果当成功 |
| A01 | 故事框架 | 世界、冲突、初始历史和开局草案 | 把未来大纲写成已发生历史 |
| A02 | NPC 画像 | 人物属性、关系、出场条件、行为依据 | 静默遗漏用户角色或固定人数 |
| A03 | 故事修订 | 世界/人物/时间线范围内的修订候选 | 越过目标范围或锁定内容 |
| A04 | 字段建议 | 单个创作 Fact 的候选 | 改 ID、版本、确认权限或其他字段 |
| A05 | 角色演绎 | NPC 的本轮行动、对白与立场 | 替用户角色做关键决定、使用未知信息 |
| A06 | 世界旁白 | 时间地点、环境、可见事件及合理反馈 | 凭空改写世界规则或提前兑现未来事件 |
| A07 | 小说转写 | 将素材融合为原创正文和结构化增量 | 偷改高优先级事实、复制作品或冒充作家 |
| A08 | 动态人物记忆 | 抽取事件/事实/关系/身份、更新画像、纠错通知 | 把模型推断冒充用户确认 |
| A09 | 摘要 | 压缩已经发生的剧情与未解线索 | 改正文或把推测升级成事实 |
| A10 | 章节归档 | 聚合已提交小说片段，保留来源 | 将聊天原文直接拼为小说或再次改写正文 |

A08 包含模型抽取和确定性记忆处理两部分。A10 默认是确定性归档工具，不必调用模型。主 Agent 默认是可测试的状态机，不额外依赖一个模型自行选择工具。

## 6. 功能需求

### FR-01 故事输入与预设

保留一个完整故事想法作为共享上下文；局部改写指令不得替换它。支持东方武侠与西方魔幻；预设是弱引导，明确用户设定优先。无模型配置时可手动编辑，但不得假装生成成功。

### FR-02 世界、人物和时间线共创

沿用第一层的两个文档与共同快照。世界持有唯一初始时间线，人物通过稳定 ID 引用。关系本体只在顶层维护。用户可编辑、确认、拒绝、锁定、增删/合并人物、处理时间线建议、导出 JSON。

自动 NPC 在世界候选校验通过后接续触发，缺失细节不阻塞候选生成；世界、时间线和人物组合结果仍须采用。失败保留已有世界，重试只生成人物。候选采用前重新校验，三方合并保留期间产生的用户修改。

### FR-03 正式写作初始化

提交已确认快照，校验结构、story_id、世界/人物/快照修订一致。要求故事想法、方向或摘要至少一项非空；不要求逐项确认、预先补齐人物或开局。被拒绝内容不是权威事实。

写作会话由服务端保存；相同快照重复初始化返回原会话，不覆盖已有正文。不同快照复用已有故事 ID 时拒绝覆盖。写作开始后的设定同步必须是独立、带版本和差异审核的操作；当前服务端版本不提供静默同步。

### FR-04 逐轮演绎与转写

输入长度 1–12000 字；每轮使用稳定 operation_id 和服务端 base_revision。按 A05→A06→A07 顺序生成。转写输出必须符合正式 Schema，时间地点记录正文实际结束场景，允许按故事线自然推进。正文按作者已有要求自主展开完整场景，并保留后续发展空间。

### FR-05 人物记忆与状态

使用初始角色 ID 建立记忆角色。正文是本轮新事件的唯一输入；用户输入、角色草案和未来规划不能直接落成已发生记忆。证据保留 sourceKind、segmentId、quote、confidence 及轮次位置。

- 正文原句验证失败时不写入记忆。
- 角色自述与旁白具有不同权威级别；身份未经可信揭示不得自动合并。
- 动态状态可更新；静态事实冲突保留待处理状态。
- 推断必须标注，模型不得自行声明 user_confirmation。
- 动态画像保留当前事实、重要关系、事件，以及不超过 100 字的核心画像与轨迹。
- 初始人物设定保留原确认/锁定语义，动态记忆通过角色 ID 作为独立覆盖层读取，不直接覆盖初始事实包装。
- 记忆中的冲突/自动纠错通知需要对用户可见；通知展示、处理和身份拆分的正式产品界面属于后续界面集成。

### FR-06 摘要与上下文

每个成功轮次更新连续摘要与未解线索。摘要上限 4000 字、未解线索最多 40 项。上下文同时包含初始设定、最近已提交片段及人物记忆。摘要不能代替权威历史；完整正文与记忆记录继续保存。

按人物、地点与输入检索记忆，在软字符预算内加入完整的近期正文片段。核心设定和权威记忆始终保留；历史请求指纹与编辑日志不进入创作上下文。不能因应用的软预算阻止开篇；供应商实际容量限制仍需明确报告，不静默截断关键设定。

### FR-07 章节归档

默认当前章累计正文达到 12000 个 Unicode 字符，或用户在本轮明确 close_chapter 时，归档该章。阈值是可调整的 MVP 默认值，不是模型上下文占用百分比。

归档保存章节号、原文、source_turn_ids、当时连续摘要与未解线索。正文按顺序连接已验证片段，包含本轮片段；摘要为连续故事摘要，不冒充独立章节文学总结。归档后下一轮进入新章。

### FR-08 恢复、幂等与一致性

每一步执行前后落盘。重复已成功操作不重复模型调用或推进版本；同 ID 不同内容报冲突。一个故事只能有一个未完成写作轮次。完成轮次时，正文、记忆日志、摘要、章节和操作状态在同一文件原子替换中提交。

模型网络结果不明与 JSON 不合法区别处理。结构修复最多一次；网络中断不自动重发。用户显式 retry_failed=true 时仅重试未成功步骤，并告知可能重复费用。

### FR-09 状态与错误展示

设定阶段沿用候选、待确认、冲突等状态。写作阶段显示各步骤 running/done/failed，以及整轮 running/blocked/succeeded。HTTP 200 不等于本轮成功，客户端必须读取 run.status。

字段级错误、修订冲突、存储损坏、故事忙碌、模型配置缺失均提供可理解文案。服务配置有效只能显示“已配置”，不能当成实时连通性证明。

### FR-10 导出与资料管理

现有世界/人物 JSON 导出继续保留。完整产品增加正文 Markdown、章节选择导出及包含版本/来源的故事备份导入。当前会话 API 已能读取正文与章节结构，专用导出/导入界面和迁移校验另行实现。

## 7. 信息架构与界面要求

| 页面/区域 | 内容与操作 | 当前交付边界 |
|---|---|---|
| 设定工作台 | 故事想法、世界、NPC、初始时间线、候选差异 | 已有界面继续复用 |
| 写作工作台 | 当前场景、正文、行动输入、步骤状态、恢复操作 | 本轮提供服务端 API，独立写作界面待接入 |
| 人物记忆面板 | 初始设定、动态画像、事件、关系、冲突与通知 | 独立验证台不等于产品界面 |
| 章节与历史 | 章节正文、来源轮次、摘要、伏笔 | 服务端结构具备，正式界面待接入 |
| 项目与导出 | 故事切换、恢复、JSON/Markdown 导出 | 世界/人物 JSON 已有；其余按阶段交付 |

延续纸张色、中文阅读字体和克制的信息密度。关键按钮有明确动作与结果；确认、锁定、来源和失败不能仅用颜色表达。移动端保持正文优先和可触达输入区，检查 390/768/1280/1440 宽度。真实运行状态不由前端计时模拟。

## 8. 数据与契约

### 8.1 初始设定

机器权威仍是三个 `0910--/schemas/*.schema.json`。Fact 保留 value/source/status/locked/evidence/updated_at。普通内容变化不改变 schema_version。稳定实体 ID 不能随名字或数组排序变化。

### 8.2 写作会话

`StorySession` 包含 version、revision、snapshot、summary、turns、chapters、memory_journal、current_time、current_location、runs。snapshot 是初始化时的已确认设定。会话 revision 从 1 开始，每成功提交一轮增加 1，与设定 snapshot_revision 分开。

`Turn` 保存 id、chapter、原始输入、转写结果、记忆结果、创建时间。`Run` 保存指纹、基础版本、原始输入、各步骤结果及尝试次数。`MemoryEntry` 保存已验证抽取、轮次输入与创建时间，用于确定性重建记忆。

世界初始时间线只记录 history/opening；新增剧情事件写入 turns 与记忆事件层。未来伏笔保留在 foreshadowing/unresolved_threads，不混入初始历史。

### 8.3 转写输入升级

`narrator_output` 统一为包含 current_time/current_location/background/visible_events 的对象。`character_output` 仍为文本。输入与输出禁用未声明顶层字段并限制长度；示例同步更新。旧的 narrator_output 字符串调用者需要迁移，不静默猜测。

## 9. 技术适配

继续采用 Next.js App Router、React、TypeScript strict、Ajv Draft 2020-12、现有测试体系。主 Agent 在服务端运行；前端不持有模型密钥。复用现有兼容 Chat Completions 的模型客户端与调用账本。

本轮服务端默认使用本机文件持久化：`0910--/runtime/stories/<story_id>.json`，包含校验和与原子重命名；目录锁排除同故事并发写。文件损坏时停止覆盖；锁不会因超时自动抢占。

人物记忆采用经过验证的抽取日志重放：复用已有 InMemoryMemoryRepository 和 CharacterMemoryAgent，以确定性 ID 与时间戳恢复，重放不调用模型。这是本地 MVP 适配。已有 PostgreSQL/pgvector 实现保留；尚未作为主 Agent 默认生产存储，不能同时写两套权威状态。

重放随历史增长，后续应引入检查点或将故事、步骤与记忆迁入同一事务数据库。不得把易失容器磁盘当长期存储。此实现没有账户隔离，默认仅适合本地可信单用户；公网发布前必须补充鉴权、owner 过滤、限流和持久卷/数据库。

## 10. 安全、可靠性与性能要求

- 模型输出和小说中的命令式文本均视为数据，不授予工具或文件权限。
- 共享同一模型请求账本；成功回放、结构修复、失败请求均可追溯。无服务端硬预算上限时不得展示虚构余额。
- 公开错误不能包含密钥、供应商响应正文或完整私密输入。
- 原始故事与逐步结果只能写产品 runtime，不能写开发 memory 文档。
- 持久化包含故事内容，应使用受控目录和备份；校验和用于发现损坏，不是加密或访问控制。
- 当前 API 同步等待顺序调用，长轮次可能超出代理超时；查询与断点恢复可用，但不是可靠后台任务队列。
- Schema 校验不能证明文学质量、人物知识边界或世界逻辑；需要真实样例评审与后续语义验证。
- 生产接入前补充输入流限额、每用户配额、成本预算、任务队列、取消语义和性能容量测试。

## 11. API 概览

统一入口：`/api/story/main`，详细字段、调用顺序和恢复方式见配套调用规则。

- GET + story_id：读取服务端会话与步骤结果。
- POST initialize：从已确认快照初始化。
- POST framework/npcs/revise/field：路由至现有设定子 Agent，结果仍是待采用候选。
- POST turn：执行或恢复同一轮剧情，可请求结束本章。

既有 `/api/story/framework`、`npcs`、`revise`、`field` 保持返回兼容，内部通过主 Agent 分派。调用者必须指定明确 action；本版不靠关键词猜测“修改设定”还是“推进剧情”。自然语言意图识别可后续增加，但不能静默产生高成本或有副作用调用。

## 12. 验收场景

| ID | Given / When / Then |
|---|---|
| AC-01 | 已确认武侠或魔幻快照 → 初始化 → 生成独立服务端会话，原快照不变 |
| AC-02 | 存在世界或人物关键问题 → NPC候选可提出待确认建议；初始化叙事使用现有条件，不因问题未答或字段未确认而拒绝 |
| AC-03 | 有效剧情输入 → 全链路 → 顺序正确，仅一次提交正文、记忆、摘要与版本 |
| AC-04 | 同操作 ID 重发 → 返回已有结果；不同内容复用 ID → 冲突 |
| AC-05 | 旁白失败 → 转写和记忆不执行；输入与角色结果保留 |
| AC-06 | 摘要失败后重启 → 显式恢复 → 复用角色/旁白/正文/记忆，只重试未完成模型步骤 |
| AC-07 | 记忆证据不在正文或引用未知人物 → 不提交记忆与正文 |
| AC-08 | 剧情自然转场 → 保存正文实际结束场景，保留原规划与事实来源 |
| AC-09 | 手动结束章或达到阈值 → 章节来源完整、正文无聊天拼接 |
| AC-10 | 两个请求同时写同故事 → 只有一个获得锁；旧版本不能覆盖 |
| AC-11 | 文件损坏 → 保留原文件并停止；不得以空故事覆盖 |
| AC-12 | 模型返回非法 JSON → 最多修复一次；网络不明 → 不自动重发 |
| AC-13 | 初始角色已有 ID → 多轮记忆重放 → 保持同一人物标识 |
| AC-14 | 真实模型多轮场景 → 人工核对人物知识、控制权、文风、身份伪装和伏笔 |

自动化 PASS 不替代 AC-14 或浏览器用户旅程验收。

## 13. 分阶段交付与依赖

1. 文档与编排基础：完整 PRD、调用规则、统一入口、缺失子 Agent 适配、断点持久化、离线集成测试。
2. 产品界面接入：初始化按钮、逐轮写作、步骤显示、历史、记忆通知、章节浏览与导出；真实浏览器验证。
3. 真实模型验收：配置文本模型，验证武侠与魔幻、多轮记忆、失败恢复；记录成本、延迟和语义问题。
4. 发布硬化：鉴权、权限、限流、数据库/备份、后台队列和恢复演练；按实际目标平台适配。

阶段一是本轮直接实现范围；阶段二至四是完整产品 PRD 的后续交付范围，不因本文落盘而标记为完成。

## 14. 风险与待验证事项

- 既有设定生成已有历史模型校验失败，需要在真实联调中复核。
- 自动记忆对叙述可信度、身份揭示和隐含事实的识别仍依赖模型质量。
- 当前连续摘要可能随长篇丢失细节，后续需结合向量检索和可解释引用。
- 缺少写作中显式修改初始设定的同步接口；不能直接把浏览器旧快照覆盖已有叙事。
- 单文件日志和同步 API 适用于本地 MVP，不等于多用户生产架构。
- 本地转写 Skill 路径属于原贡献者机器，运行时采用本项目提示词；不声称已经安装该 Skill。
- 参赛提交平台和时间要按最新赛事通知确认；不得用通用部署手册覆盖具体赛事要求。


## 本地版本交付补充（2026-09-12）

当前实现的入口为故事设定 → 开始写作 → 正文/人物与记忆/章节与伏笔/同步设定，以及我的作品列表。具体完成度与验收证据仅在 memory/CURRENT.md 维护。

备份默认不覆盖相同故事 ID；写作中的设定同步不允许增删人物 ID。人工身份修正、事实冲突选择有独立记录。标为已读仅处理通知，不自动解决冲突。费用估算缺少价格或 token 时应明确未知，不显示为免费。

本地任务响应后执行，用户可以离开网页；重启后须明确恢复。公网账户系统、数据库部署、HTTPS 和发布材料取决于目标环境，不能用本机验收替代。

### 当前自动创作体验（OP-20260912-012）

开始写作后直接生成饱满开篇。无需逐项确认；用户可留空续写要求，作者也可指定新方向。应用接入jin-yong-perspective创作方法设计人物成长、冲突、伏笔与节奏，正文不设固定字数，以场景完整性为准；不宣称突破模型单次容量。存储、版本和JSON契约检查不等于创作前置限制，仍用于保护既有作品。


### 生成速度与完整性补充（2026-09-12）

正常叙事轮次使用4次模型请求：人物/故事线统筹、正文、记忆抽取、摘要。角色资料、关系索引等可确定的数据由本地处理。正文无新增固定字数限制；缩短的是重复调用和冗余上下文。正文先展示流式预览，记忆和摘要结束后才成为正式轮次；失败保留可恢复结果。实际供应商延迟、流式能力及文学质量需单独验收，调用减少比例不能等同于耗时减少比例。


### 直接生成正文（OP-20260912-016）

新剧情轮次将人物演绎、故事线与正文合并成首个模型请求，后台继续记忆抽取和摘要，共3次正常调用。正文流式阅读不等待后台整理，正文篇幅仍按场景完整性决定。旧未完成轮次保留原协议恢复；新流程不强行重写旧回执。


### FR-互动对话：用户扮演主角（2026-09-13）

用户以主角身份参与故事。叙事推进到NPC需要主角回答的台词即停止，正文显示待生成，提供NPC对话与回应输入；不能替用户主角作答或作关键决定。用户可说话、拒绝、追问或描述行动，系统结合背景、当前人物与记忆生成真实后果，再推进至下一对话。NPC之间的普通对白不必暂停，不能机械地以引号判断停点。

验收：停点持久保存；空回答和过期对话不能推进；回应原文进入后续正文与来源明确的记忆；刷新不自动生成；失败恢复保留停点与回答；新停点替代旧停点；备份恢复不丢失对话。维持每轮三次正常模型调用，正文篇幅按完整场景决定，但必须在需要用户回应的位置停笔。


### 2026-09-13 故事想法驱动填充

“生成世界、画像与时间线”以当前故事想法为主输入，模型识别并扩充世界、人物和前史/开局。预设仅为风格提示，不能固定人物地点和情节。通过校验的组合结果自动填入草稿，保留来源及未确认状态；NPC前史建议同时进入时间线。局部字段修改仍预览，手动锁定和并发编辑冲突保留保护。旧聊天输入不作为整体重生成的附加指令。


### 独立多轮NPC交流（OP-20260913-019）

对话不再等待整轮记忆/摘要完成。正文到问话处即暂停并弹出独立窗口；用户多次发送只与NPC交流，不逐次生成正文。自然结束或明确结束后才汇入交流原文、整理记忆并自动续写，再次遇到对话重复同一流程。关闭窗口不退出对话。刷新/备份/失败恢复保持角色、消息与接续位置。设计及可执行验收见docs/NPC_DIALOGUE_AGENT.md。
