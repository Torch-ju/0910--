# 书中人：项目协作规则

本文件是本项目唯一的 Agent 协作入口。文件名统一为 `AGENTS.md`，不另建 `agent.md`、`AGENT.md` 或嵌套规则副本。这里仅补充项目事实、文档职责与接续方法；用户提供的全局协作规则继续适用。

## 产品与范围入口

产品本意及第一层需求只维护在 [功能规格 SPEC-L1](<D:/研究生学习资料/竞赛/20260911 黑客松比赛/docs/PRODUCT_SPEC.md>)。所有开发必须能对应其中的功能 ID。已定决策见 [DECISIONS](<D:/研究生学习资料/竞赛/20260911 黑客松比赛/memory/DECISIONS.md>)。不要把早期完整产品架构中的未来章节、长记忆或账号系统自动带入第一层。

项目 `memory/` 是唯一开发记忆库；它服务于开发接续，不承担产品用户的故事记忆。

## 唯一职责

| 内容 | 唯一写入位置 | 维护时机 |
| --- | --- | --- |
| 项目规则与恢复协议 | `AGENTS.md` | 协作规则发生变化 |
| 项目入口与可复现启动命令 | `README.md` | 入口或实际命令变化 |
| 产品本意、功能、UI 与数据契约需求 | `docs/PRODUCT_SPEC.md` | 用户需求或具体设计变化 |
| 记忆文件与主题索引 | `memory/INDEX.md` | 文件或定位方式变化 |
| 当前任务、进度、阻塞、下一步、授权 | `memory/CURRENT.md` | 有实质进展或用户改变方向 |
| 决策依据和被替代关系 | `memory/DECISIONS.md` | 作出或改变一个决定 |
| 开发事件、文件变化、检查结果 | `memory/HISTORY.md` | 完成或中断一个有意义的工作单元 |

README 不复制任务状态表，功能规格不勾选实现完成项，历史记录不作为当前状态使用。禁止建立另一套 CURRENT、TODO、handoff、开发日记或全局记忆副本。

## 进入当前对话或接续新对话

1. 读取本文件和 `README.md`，确定文档职责与项目入口。
2. 读取 `memory/INDEX.md` 与 `memory/CURRENT.md`，定位当前任务、最后操作 ID、授权和下一步。
3. 按索引读取对应功能和决定；先确认产品本意与首期边界，再只读当前任务相关材料。
4. 依据最新历史事件核对有关文件。时间戳、旧状态和历史聊天都不能单独证明功能完成或进程仍运行。
5. 如果记录与工件冲突，标为“待核实”，先做局部只读核对；保留已有工作，不重建整套项目，也不从零再问全部产品问题。
6. 在当前授权范围内继续未完成工作。授权缺少的真正外部操作或范围改变才请求用户决定。

无需为了继续开发创建新对话。若用户自主切换到另一个 AI，要求其先读取本入口；不依赖它自动拥有旧对话。恢复的依据是落盘文档与实际工件。

## 进度状态和证据

任务 ID 使用 `L1-00` 至 `L1-06`；后续新增时使用新 ID，不重用旧 ID。状态使用 `TODO`、`IN_PROGRESS`、`BLOCKED`、`DONE`。

`DONE` 必须同时有真实交付物和与其性质相称的检查证据。只写了规格不能说明 Schema 已创建，只做了演示不能说明真实模型已接入。每次检查区分 `PASS`、`FAIL`、`NOT_RUN`。缺少运行条件的部分保留具体状态，不隐藏在“整体完成”中。

开发代码、演示数据、真实模型联调和可发布产品分别记录。当前事实以 `CURRENT.md` 为准，不在这里复制会过期的初始化状态。

## 幂等更新与中断恢复

每个有意义的工作单元先选定一个 `operation_id`，例如 `OP-20260912-001`。在 CURRENT 记录目标、影响文件和当前状态；HISTORY 同 ID 只建一个事件。

操作状态使用 `STARTED`、`APPLIED`、`CHECKED`、`FAILED`。重试同一操作时更新原事件，不重复追加一条“完成”；仅阅读、等待或无变化不创建事件、不推进修订号。新目标用新 ID，同 ID 不承载不同改动。

写入前先读取当前记录；同一主 Agent 串行维护这些共享文件。子 Agent 可以提交证据摘要，但不并发修改 CURRENT、DECISIONS、HISTORY 或索引。

工作落盘后按顺序更新：工件 → HISTORY 的结果和证据 → CURRENT 的状态与下一步 → 必要的索引/README。这是可恢复的串行流程，不宣称多文件写入具有数据库原子性。

若中断，依据 operation_id、历史记录和实际工件补齐缺失步骤。存在未完成事件时先判断已应用范围，再继续；不重做已成功的操作。CURRENT 引用的事件尚未成功时，不把任务标记 DONE。

状态记录是检查点，不是备份。历史中的关键判断保留；被替代决定注明后继 ID，禁止静默改写其原理由。任何文件删除或历史清理遵守用户明确审批。

## 开发与资料维护

数据需求以 SPEC-L1 为准。独立 `schemas/*.schema.json` 创建后成为机器校验入口；类型、前端、Provider 输出与校验器必须对应同一契约。Schema 变化同步需求描述、样例及相关决定，不用另一份 JSON 示例替代它。

测试、源码入口和命令只在真实存在且实际使用后登记。操作系统问题记录具体报错和已成功的处理路径；不要把旧工具缓存绝对路径当成永远有效的项目依赖。

项目目录中的早期架构和技术手册保留为资料。已有实现不存在时，不继承其提到的其他项目路径。对不在本轮范围内的资料不重复转换和全面检索。

每个工作单元结束或上下文即将压缩前，更新 CURRENT 中的“下一项具体动作”和最后事件 ID。用户只问概念且没有改变决定或进度时，无需制造新的记忆记录。

这些文档由执行开发的 Agent 按工作单元维护；当前没有后台同步服务或自动监控。不要声称文件能自动保存未落盘的聊天。

Next.js 配置显式禁用自动生成 Agent 规则（agentRules: false），保持本文件的人工维护权威。启动脚本仅在进程中禁用遥测，避免宿主全局配置写入；不改用户全局工具配置。
