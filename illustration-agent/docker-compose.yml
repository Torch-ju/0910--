services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: illustration_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - illustration-agent-pg:/var/lib/postgresql/data

volumes:
  illustration-agent-pg:
