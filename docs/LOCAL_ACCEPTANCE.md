# 本地版本验收报告

日期：2026-09-12；工作单元：OP-20260912-007。

本地写作与作品管理功能已实现并完成自动化验证。真实创作取得武侠三轮、魔幻两轮完整成功证据；两种风格均三轮成功的验收断言仍为 FAIL，不能将本报告理解为全部计划完成。

## 工程证据

| 检查 | 结果 |
| --- | --- |
| 主应用离线测试 | 108 PASS，3 项付费/可选测试跳过 |
| 独立 memory-agent 测试 | 12 PASS |
| Lint、TypeScript、Schema 类型一致性、生产构建、diff 空白检查 | PASS |
| 可重复浏览器 test:e2e | PASS；初始化、连续两轮、刷新无额外调用、摘要失败恢复仅一次调用、下一轮草稿保留、章节/作品/记忆、Markdown 内容 |
| 响应式 | 375 / 768 / 1280 / 1440 宽度无横向溢出；移动界面已查看截图 |
| 干净环境 | 源码副本 npm ci 安装 473 包、测试和构建通过；此后小幅修复在工作仓库通过最终全套检查 |

浏览器使用隔离合成模型，11 次合成调用，不作为真实模型质量证据。原生下载事件等待曾超时，最终验证的是 Markdown 内容，不包含操作系统下载落盘保证。

## 真实模型结果

所有原始输出、校验错误、修复回执与故事保存在被忽略的 runtime/evaluation。未设置模型单价，费用统计为 null；未知网络结果不代表没有计费。

- 基线：2026-09-12T12-47-52.948Z。两种世界生成成功，NPC 包装结构失败。根因是提示仅传递定义片段，遗漏外层对象；现已传完整 Schema 并增加回归测试。
- 修正后：2026-09-12T12-57-14.223Z。复用成功世界，两种 NPC 均通过，叙事使用实际生成的世界和人物。
- 针对性恢复：同目录 recovery-*-report.json 保留各次失败。最后 recovery-2026-09-12T13-09-44.426Z-report.json：武侠由 2 轮恢复至 3 轮成功；魔幻保持 2 轮，第三轮记忆请求网络结果未知。没有再次生成已完成的角色、旁白或正文。
- 记忆曾出现空人物提示 ID、空参与者数组及不连续证据引文。现在单次修复同时收到结构错误和业务证据错误，仍严格拒绝无效引文；未使用静默删改来伪造成功。

## 人工语义评审

| 维度 | 结论和实例 |
| --- | --- |
| 人物和场景 | 已成功轮次姓名保持；武侠位于雨夜客栈，魔幻位于暮光塔二楼走廊。武侠检查门锁后位置字段仍写柜台前，精度不足 |
| 用户控制 | 未打开用户明确保留的信，也未替用户选择住宿；武侠第一轮补写了问候台词，第三轮扩写了触摸和转身动作，控制边界仍需收紧 |
| 意图与结果 | 检查门锁没有凭空找到重要线索，但正文“确认每一寸”及结尾推断昨夜门未开，超出检查证据支持范围 |
| 信息来源 | 魔幻第二轮记忆 journal 将蓝光、导师位置等标为 character_statement；但 prose.new_facts 和画像事件文本省略来源，容易被读成确定历史。武侠第三轮把谨慎性格推断标成非推断，尚未解决 |
| 隐藏信息 | 已读成功正文未泄漏信件具体内容；对信的反复注视和暗示较多 |
| 伏笔与文风 | 未解线索分栏保存；重复灯火、目光、雨声及悬念收尾较明显，尚不能认为文风稳定 |
| 长篇 | 100 片段离线预算与完整历史保留通过；没有真实长篇样本，不能推断数十章摘要保真 |

最新上下文增加事实来源记录；该补丁离线通过，但现有已生成正文并未重新生成，不能声称上述语义缺陷都已修复。

## 交付范围与限制

已交付写作工作台、持久会话恢复、步骤状态、故事列表、人物证据/冲突/身份修正、章节、Markdown、完整备份校验、版本同步与放弃审计。加入上下文字符预算和相关记忆检索、检查点、账本成功回执重用、死进程锁检查、调用统计及可选预算。

后台任务使用本地 Next after 和持久任务状态；进程重启后识别中断，显式恢复，不是跨进程任务队列。字符预算不是模型精确 token 预算。多进程账本事务、数据库部署、真实长篇压力和全面磁盘故障矩阵未验收。停止等待不取消执行；取消在当前模型调用结束后的步骤边界生效；放弃保留审计。

公网登录、权限、限流配额、部署存储迁移、监控、HTTPS 与回滚未实施。仓库尚未提交推送；原有压缩包删除和展开目录保留。参赛材料需有具体赛事后再核实。
